# État d'avancement — SAFRAN TIME TRACKER

> Ce document reflète l'état **réel** du projet, à distinguer de `docs/ROADMAP.md` qui décrit le plan. Il doit être mis à jour à la fin de chaque lot (voir `CLAUDE.md` §20 — Méthode de travail par lots), et à chaque écart significatif constaté en cours de lot.

**Dernière mise à jour : 2026-07-22 — Lot 13 (Authentification, RBAC, Sécurisation API, CI/CD et Qualité) terminé.**

## Phase préliminaire — Socle documentaire

**Statut : Terminé (2026-07-15).**

Cette phase n'est pas un lot numéroté du cahier des charges (§40) : elle précède le `Lot 0 — Fondations`. Elle a couvert uniquement la production et la validation de la documentation de cadrage (voir « État détaillé » ci-dessous).

## Synthèse des lots

| Lot | Contenu | Statut | Dernière mise à jour |
|---|---|---|---|
| Lot 0 | Fondations (périmètre technique restreint — voir ci-dessous) | Terminé | 2026-07-15 |
| Lot 1 | Référentiels | Terminé | 2026-07-15 |
| Lot 2 | Modèle financier | Terminé | 2026-07-15 |
| Lot 3 | Temps et capacité | Terminé | 2026-07-15 |
| Lot 4 | Projets | Terminé | 2026-07-15 |
| Lot 5 | Budgets et reporting | Terminé | 2026-07-15 |
| Lot 6 | Imports et audit | Terminé | 2026-07-17 |
| Lot 7 | Frontend Foundation (Design System) | Terminé | 2026-07-17 |
| Lot 8 | Référentiels et Administration | Terminé | 2026-07-20 |
| Lot 9 | Temps et Disponibilités | Terminé | 2026-07-20 |
| Lot 10 | Projets et Planning | Terminé | 2026-07-21 |
| Lot 11 | Commandes, Budgets et Jalons | Terminé | 2026-07-21 |
| Lot 12 | Charges, Tableau de bord, Reporting et Imports | Terminé | 2026-07-21 |
| Lot 13 | Authentification, RBAC, Sécurisation API, CI/CD et Qualité | Terminé | 2026-07-22 |
| Lot 14 | Audit, Refactoring et Qualité | Non démarré | — |
| Lot 15 | Complétude fonctionnelle & Administration | Non démarré | — |
| Lot 16 | UX, Responsive et Performance frontend | Non démarré | — |
| Lot 17 | Industrialisation et packaging (ancien Lot 15, ancien Lot 14, ancien Lot 13, ancien Lot 7) | Non démarré | — |
| Lot 18 | Pré-production | Non démarré | — |
| Lot 19 | Mise en production V1 | Non démarré | — |

## État détaillé

### Documentation
- [x] Cahier des charges fonctionnel intégré au dépôt (`docs/`).
- [x] `CLAUDE.md` (mémoire technique permanente) créé.
- [x] `README.md` créé.
- [x] `docs/ARCHITECTURE.md` créé.
- [x] `docs/DATABASE.md` créé.
- [x] `docs/CONVENTIONS.md` créé.
- [x] `docs/ROADMAP.md` créé.
- [x] `.gitignore` créé.

### Code — Lot 0 (2026-07-15)

- [x] Dépôt Git initialisé (fait à la clôture de la phase documentaire).
- [x] Solution .NET créée : `backend/SafranTimeTracker.slnx` (nouveau format `.slnx`, par défaut depuis le SDK .NET 10).
- [x] Projets Clean Architecture créés et référencés conformément à `docs/ARCHITECTURE.md` §2 : `SafranTimeTracker.Domain`, `SafranTimeTracker.Application`, `SafranTimeTracker.Infrastructure`, `SafranTimeTracker.Api`, `SafranTimeTracker.Tests`. Aucune entité métier, aucun contrôleur métier, aucune page fonctionnelle — conformément à la demande explicite.
- [x] Entity Framework Core configuré : `AppDbContext` (vide, sans DbSet), sélection du provider par configuration (`Database:Provider`), mapping snake_case uniforme (`EFCore.NamingConventions`).
- [x] PostgreSQL, SQLite et SQL Server configurés comme providers EF Core, chacun avec son propre projet de migrations (`database/{sqlite,postgresql,sqlserver}/SafranTimeTracker.Migrations.*`, pattern officiel Microsoft pour éviter tout mélange de migrations entre providers).
- [x] Migration initiale (vide) générée et vérifiée pour les **trois** providers. Appliquée réellement (`dotnet ef database update`) et validée sur SQLite ; PostgreSQL et SQL Server générés avec succès en mode design-time uniquement (aucune instance locale disponible pour tester une connexion réelle — voir écarts).
- [x] Serilog (journalisation structurée) configuré : console + fichier par environnement (Qualification/Production → `E:/data/logs/SafranTimeTracker/api/`).
- [x] Swagger/OpenAPI configuré (Swashbuckle.AspNetCore), document `v1` valide, aucun endpoint métier documenté.
- [x] Endpoint technique `/health` exposé (health checks ASP.NET Core natifs, sans dépendance métier).
- [x] CORS configuré par environnement (`Cors:AllowedOrigins`), vide par défaut hors développement.
- [x] `appsettings.json` + variantes `Development` / `Qualification` / `Production` créées (aucun secret réel).
- [x] Projet de tests `SafranTimeTracker.Tests` créé (xUnit + FluentAssertions + NSubstitute + Mvc.Testing) avec 2 tests d'intégration d'infrastructure (`/health`, `/swagger/v1/swagger.json`) — 2/2 réussis.
- [x] Frontend créé : `frontend/safran-time-tracker-web` (React 19 + TypeScript strict + Vite), avec React Router, TanStack Query, React Hook Form, Zod, Recharts, axios installés conformément à `CLAUDE.md` §4. Squelette de dossiers (`api/`, `components/ui/`, `features/`, `hooks/`, `routes/`, `lib/`) créé, une seule page technique de vérification (aucun écran fonctionnel).
- [x] ESLint (flat config) + Prettier configurés côté frontend.
- [x] `Directory.Build.props` (racine) + `.editorconfig` créés.
- [x] Scripts PowerShell créés et testés de bout en bout : `deploy/windows/powershell/install.ps1`, `build.ps1` (option `-RunTests`), `publish.ps1`.
- [x] Vérification finale : `dotnet build` (solution complète, 3 projets de migrations inclus) → 0 avertissement, 0 erreur ; `dotnet test` → 2/2 ; `npm run lint` / `npm run format:check` / `npm run build` → tous verts.

### Code — Lot 1 (2026-07-15)

- [x] Entités Domain créées, conformément à `docs/DATABASE.md` §4 et au cahier des charges : `Department`, `Service`, `Team` (§9), `Resource`, `OperationalRole`, `ResourceOperationalRole` (§10), `Company`, `CompanyType` (§12.1), `Order`, `OrderStatus`, `OrderAuthorizedResource` (§13), `ApplicationReference`, `ApplicationCriticality` (§15), `User`, `Role`, `Permission`, `UserPermission` (§10.2, §6.2), `Settings` (§28.2). Base commune `AuditableEntity` / `ReferentialStatus` (statut plutôt que suppression physique, `CLAUDE.md` §7).
- [x] Configuration EF Core par entité (`IEntityTypeConfiguration<T>`) sous `Infrastructure/Persistence/Configurations/`, mapping snake_case uniforme, conformément à `CLAUDE.md` §11.
- [x] Persistance générique : `IRepository<T>` / `IReadRepository<T>` (`Application/Common/Persistence`) implémentées par `EfRepository<T>` (`Infrastructure/Persistence/EfRepository.cs`) — aucun dépôt dédié par entité, le Lot 1 ne contenant aucune règle métier de recherche complexe (chevauchement, date de saisie, etc.) qui le justifierait.
- [x] 9 contrôleurs REST sous `/api/v1` (`kebab-case`, pluriel) : `departments`, `services`, `teams`, `users`, `resources`, `applications`, `companies`, `orders`, `settings`. Pagination serveur (`PagedResult<T>`, `PaginationQuery`) sur toutes les listes, conformément à `CLAUDE.md` §12.
- [x] Validation serveur systématique via FluentValidation (un validateur par `XxxCreateRequest`/`XxxUpdateRequest`), rejouée côté API indépendamment de toute validation client (`CLAUDE.md` §7) ; `FluentValidation.DependencyInjectionExtensions` ajouté pour l'enregistrement automatique des validateurs (`Application/DependencyInjection.cs`).
- [x] Mapping DTO ↔ entité via Mapster (`OrderMapsterConfig`, `ResourceMapsterConfig`, `UserMapsterConfig`) pour les projections non triviales.
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot1Seed.cs`, via `HasData`, données et horodatage strictement déterministes) : 1 département (DSI), 4 services, 2 équipes, les 13 utilisateurs/ressources nommés au cahier des charges §5.4, la société interne SAFRAN, une commande de démonstration, 3 applications (IBM ELM, VTOM, ServiceNow), les référentiels de rôles/permissions/rôles opérationnels/statuts de commande, et les paramètres par défaut. Ne couvre pas le jeu de données volumétrique complet du §35 (dépend d'entités non encore créées : `Project`, `TimeEntry`, etc. — hors périmètre Lot 1).
- [x] Migration `AddLot1Referentials` générée pour les **trois** providers (`database/{sqlite,postgresql,sqlserver}/Migrations/`). Appliquée et validée réellement pour SQLite via les tests d'intégration (`WebApplicationFactory` + `dotnet ef` implicite au démarrage) ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — même écart qu'au Lot 0 (voir « Écarts connus »).
- [x] `Program.cs` : câblage de `builder.Services.AddApplication()` (validateurs + Mapster) ; correction d'un bug de démarrage (bootstrap logger Serilog à deux phases incompatible avec `WebApplicationFactory`, qui réexécute `Program` pour chaque instance d'hôte de test — voir « Décisions actées »).
- [x] Tests : 20 nouveaux cas ajoutés (13 tests d'intégration API sur les 7 référentiels dans `ReferentialsTests.cs` — liste seedée, création valide, rejet d'une création invalide, 404 — + 2 tests unitaires de validateur avec 5 cas `[Theory]` dans `SettingsUpdateRequestValidatorTests.cs`). Suite complète : **22/22 réussis** (20 nouveaux + 2 tests d'infrastructure du Lot 0).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète, 8 projets) → 0 avertissement, 0 erreur ; `dotnet test` → 22/22 ; `npm run build` (frontend, inchangé ce lot) → succès ; `npm run lint` (frontend) → 0 erreur.

### Code — Lot 2 (2026-07-15)

- [x] **`ICurrentUser`** (`Application/Common/Security/ICurrentUser.cs`) : seule abstraction dont dépendent contrôleurs et services pour vérifier une permission ou tracer un auteur (`IsAuthenticated`, `UserId`, `Identifier`, `Permissions`, `HasPermission`). Implémentation active **`DemoCurrentUserProvider`** (`Api/Security/`) : résout l'utilisateur depuis l'en-tête `X-Demo-User` (identifiant), vérifié en base contre `User`/`UserPermission`/`Permission` réels — explicitement **pas** un login, pas de JWT, pas de session, pas de cookie, pas ASP.NET Identity (demande explicite). Enregistrée en un seul point (`Program.cs`) : remplaçable par un provider LDAP/OIDC futur sans modifier un contrôleur, un service métier ou une règle de sécurité.
- [x] Garde de permission **`RequirePermissionAttribute`** (`Api/Security/`) : filtre d'autorisation MVC (`IAsyncAuthorizationFilter`) ne dépendant que d'`ICurrentUser` — volontairement pas le système `Authorization`/`ClaimsPrincipal` d'ASP.NET Core, pour rester strictement découplé de toute notion d'authentification. Posée sur les 4 contrôleurs financiers ; sans la permission `FINANCIAL_DATA_VIEW`, le contrôleur n'est jamais atteint (403), jamais un simple masquage côté client (`CLAUDE.md` §12, §17).
- [x] Entités Domain historisées, conformément à `docs/DATABASE.md` §5 et au cahier des charges : `ResourceTjmHistory` (§11), `CompanyContractHistory` (§12.3), `ResourceCompanyAssignment` (§12.2, remplace `Resource.CompanyId` du Lot 1 comme source de vérité pour le calcul financier — `Resource.CompanyId` reste un pointeur d'affichage non historisé). Jeton de concurrence optimiste (`ConcurrencyStamp`, type `Guid` géré applicativement, portable sur les 3 providers) sur TJM et contrat, conformément à `CLAUDE.md` §11.
- [x] **`FinancialCalculationService`** (`Application/Financial/Services/`), seul point de calcul (`docs/ARCHITECTURE.md` §2) : `tempsJours = heuresSaisies / heuresParJour` (§20.1), `coutReel = tempsJours × tjmPersonneSnapshot` (§20.2), `coutContractuel = tempsJours × tjmContratSnapshot` si société externe avec contrat valide, sinon non applicable (§12.5, §20.3), `differentiel = coutContractuel - coutReel` (§20.4). Recherche du TJM/contrat/rattachement **à la date demandée**, jamais à la date courante. Absence de TJM → `FinancialValuationStatus.Incomplete`, aucun montant inventé (§11.4). Ne persiste rien : `TimeEntryFinancialSnapshot` référence `TimeEntry` (Lot 3) et reste différé — ce service est directement réutilisable tel quel par le `TimeEntryService` du Lot 3.
- [x] Conflits métier (chevauchement de périodes, concurrence optimiste) traduits en **409** via `BusinessConflictException` + `BusinessConflictExceptionHandler` (`IExceptionHandler`, `Api/Middleware/`) — jamais un 400, conformément à `CLAUDE.md` §12. Validation de format/existence FK/montant strictement positif via FluentValidation (400) ; chevauchement vérifié côté service (409).
- [x] 4 contrôleurs REST sous `/api/v1`, tous gardés par `FINANCIAL_DATA_VIEW`, endpoint entièrement refusé (403) plutôt qu'omission de champ à la pièce (ces endpoints sont exclusivement financiers) : `resource-tjm-history`, `company-contracts`, `resource-company-assignments` (GET liste/détail, POST création, PUT correction/clôture avec motif obligatoire), `financial-calculations/preview` (POST, calcul à la demande sans écriture).
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot2Seed.cs`) : une société externe (Externe Conseil) et son contrat, 4 historiques TJM dont un cas d'historisation (BERNARD ouvert société interne, LEGRAND ouvert société externe avec contrat, GEORGES deux périodes successives non chevauchantes), MISHRA volontairement sans TJM (cas de valorisation incomplète, `docs/DATABASE.md` §7), 3 rattachements ressource/société.
- [x] Migration `AddLot2FinancialModel` générée pour les **trois** providers. Appliquée et validée réellement pour SQLite (`dotnet ef database update` en chaîne sur les 3 migrations + tests d'intégration `WebApplicationFactory`) ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — écart reconduit du Lot 0/1.
- [x] Tests : 16 nouveaux cas ajoutés (5 tests unitaires purs `DateRangeOverlapTests` — chevauchement/adjacence/inclusion, aucune base de données, logique partagée par les 3 historiques — + 11 tests d'intégration `FinancialModelTests` sur base SQLite réelle : 403 sans en-tête, 403 avec permission manquante, 200 avec données financières correctes, calcul société interne/externe/valorisation incomplète, historisation TJM dans le temps, 409 chevauchement, 400 montant invalide, clôture puis nouvelle période). Aucune règle financière testée sur repository mocké (`CLAUDE.md` §14). Suite complète : **38/38 réussis** (16 nouveaux + 22 des Lots 0/1).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète, 8 projets) → 0 avertissement, 0 erreur ; `dotnet test` → 38/38 ; `npm run build` (frontend, inchangé ce lot) → succès ; `npm run lint` (frontend) → 0 erreur.

### Code — Lot 3 (2026-07-15)

- [x] **`ActivityType`** (§19.2, §30, entité obligatoire) enrichie de métadonnées de validation (`ReferenceRequired`, `ReferenceFormatRegex`, `ReferenceExample` — demande explicite) : la validation de référence d'une saisie (§19.3) est entièrement pilotée par la donnée, aucun type d'activité codé en dur dans un validateur. Classification RUN/hors RUN (§29.4) portée par `ActivityType.IsRun`, seedée pour les 13 types.
- [x] **`TimeEntry`** (§19.1) + **`TimeEntryFinancialSnapshot`** (§19.5, enfin persistée — différée depuis le Lot 2) : `TimeEntryService` orchestre la création/modification et revalorise via `FinancialCalculationService` (Lot 2, réutilisé sans modification de sa logique de calcul, seulement enrichi de `GetApplicableCompanyIdAsync` pour éviter une duplication de la recherche société-à-date, et de `SourceTjmPersonne`/`SourceContrat` désormais renseignés). Sous-objet financier du DTO omis sans `FINANCIAL_DATA_VIEW` — projection faite dans le service (`CLAUDE.md` §13), premier DTO « mixte » du projet (financier + non financier), contrairement aux endpoints intégralement financiers du Lot 2.
- [x] Règles métier bloquantes en 409 (`BusinessConflictException`) : ressource inactive, période close (via `Settings.DelaiModificationTempsJours`, Lot 1 — aucune nouvelle entité), incompatibilité commande/société à la date de la saisie (§13.4, désormais vérifiable).
- [x] **`ITimeEntryRevaluationService`** (`Application/Financial/Services/`) : interface posée dès ce lot (demande explicite) pour préparer le recalcul explicite audité du Lot 6 (§19.6) sans refonte d'architecture ; implémentation `TimeEntryRevaluationService` volontairement `NotImplementedException` (dépend d'`AuditLog`, Lot 6), enregistrée en DI — seul le corps de la méthode sera remplacé au Lot 6.
- [x] **`Absence`** (§23) : workflow Brouillon→Soumis→Validé/Refusé/Annulé (§23.3), piloté par `Settings.ActivationValidationAbsences` (Lot 1) — soumission avec workflow désactivé vaut validation immédiate, de sorte qu'`AvailabilityService` n'a besoin de filtrer que sur le statut Validé sans connaître ce paramètre. `AbsenceType`/statut modélisés en enum C#, absents de la liste d'entités minimum du §30 (contrairement à `ActivityType`) — correction d'une imprécision de l'écart Lot 1 qui les y annonçait à tort.
- [x] **`ResourceCapacityPeriod`** (§10.5, §30, entité obligatoire) et **`HolidayCalendar`** (§22.2, §30, entité obligatoire) créées — les deux étaient différées depuis le Lot 1.
- [x] **`AvailabilityService`** (`Application/Capacity/`) : capacité théorique/réelle/taux de disponibilité (§29.1-29.3, calcul jour par jour, variations de capacité + jours fériés + absences validées) et agrégation charge RUN/hors RUN (§29.4, pilotée par `ActivityType.IsRun`). `GetApplicableDailyCapacity` extraite en fonction pure publique, testée unitairement sans base de données (`CLAUDE.md` §14).
- [x] 6 contrôleurs REST sous `/api/v1` : `time-entries`, `absences` (+ `submit`/`validate`/`refuse`/`cancel`), `activity-types`, `resource-capacity-periods`, `holiday-calendar`, et `resources/{resourceId}/availability` (sous-ressource de composition forte, `docs/CONVENTIONS.md` §4). Aucun n'est gardé par `FINANCIAL_DATA_VIEW` au niveau contrôleur : la protection financière de `time-entries` est au niveau champ (voir ci-dessus), les autres ne portent aucune donnée financière.
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot3Seed.cs`) : 13 types d'activité, 5 jours fériés français, une variation de capacité (temps partiel), 6 saisies de temps couvrant les cas notables (société interne, société externe avec différentiel positif, historisation TJM dans le temps sur la même ressource, valorisation incomplète), 4 absences dans des statuts différents, une ressource inactive dédiée au test du blocage §19.4.
- [x] Migration `AddLot3TimeAndCapacity` générée pour les **trois** providers. Appliquée et validée réellement sur SQLite ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — écart reconduit.
- [x] Tests : 21 nouveaux cas ajoutés (4 tests unitaires purs `AvailabilityCapacityTests` sur `GetApplicableDailyCapacity` + 16 tests d'intégration `TimeAndCapacityTests` sur base SQLite réelle : snapshot correct interne/externe/incomplet, sous-objet financier omis sans permission, absence de recalcul rétroactif démontrée par l'historisation TJM au sein même des saisies, 409 commande/société incompatible, 409 ressource inactive, 409 période close, 400 référence manquante/format invalide, revalorisation après modification, workflow absence complet, 409 transition invalide, calcul de capacité/charge RUN-hors-RUN via l'API, 404 ressource inconnue — + 1 test `AbsenceWorkflowDisabledTests` isolé dans sa propre base pour la validation automatique quand le workflow est désactivé). Un bug de suivi EF Core a été détecté et corrigé pendant l'écriture des tests (voir « Décisions actées »). Aucune règle capacitaire testée sur repository mocké (`CLAUDE.md` §14). Suite complète : **59/59 réussis** (21 nouveaux + 38 des Lots 0-2).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète, 8 projets) → 0 avertissement, 0 erreur ; `dotnet test` → 59/59 ; `npm run build` (frontend, inchangé ce lot) → succès ; `npm run lint` / `npm run format:check` (frontend) → 0 erreur.

### Code — Lot 4 (2026-07-15)

- [x] Entités Domain créées, conformément à `docs/DATABASE.md` et au cahier des charges : `ProjectStatus` (référentiel administrable, même schéma qu'`OrderStatus`), `Project` (§16, ne stocke aucun agrégat de charge/coût calculé — toujours calculé à la demande), `ProjectParticipant` (§17.2, période Début/Fin, rôle opérationnel et commande par défaut **au périmètre du projet**, distincts de ceux de `Resource`), `ProjectPlanVersion`/`ProjectWeeklyPlan` (§18.3, granularité hebdomadaire), `MilestoneType` (référentiel administrable, §24.1) et `Milestone` (§24, dépendance optionnelle vers un autre jalon, sans détection de cycle). `TimeEntry.ProjectId` (nullable) ajouté au Lot 3 pour clôturer le lien saisie de temps ↔ projet.
- [x] Cycle de vie des versions de planning (§18.3), précision actée avec l'utilisateur à la validation du plan : une seule version Initiale par projet (immuable une fois créée), plusieurs versions Ajustées possibles, une seule Active à un instant donné — la précédente Ajustée Active bascule **automatiquement** à Archivée dès qu'une nouvelle devient Active (`ProjectPlanningService.CreateAdjustedVersionAsync`), aucune suppression physique, historique intégralement conservé. Vérifié par un test d'intégration dédié.
- [x] **`ProjectPlanningCalculator`** (`Application/Projects/Services/`), fonctions pures sans accès base de données (`CLAUDE.md` §14) : `CalculateChargeMetrics` (chargeRestante/écartCharge/dériveCharge/atterrissageCharge, §29.5 — « prévu » = chargeAjustée sinon chargeInitiale), `CalculatePlanningRisk` (dérivePlanningJours/risquePlanning, risque uniquement si la date ajustée dépasse l'initiale), `CalculateBudgetRisk` (coût réel consommé > budget initial). Seul `ProjectPlanningService.GetSynthesisAsync` les invoque — aucune règle dupliquée ailleurs, même principe que `FinancialCalculationService` (Lot 2) et `AvailabilityService.GetApplicableDailyCapacity` (Lot 3).
- [x] Protection financière à deux niveaux, reconduite et étendue : `ProjectDto.FinancialSummary`, `ProjectParticipantDto.FinancialSummary` et `ProjectPlanningSynthesisDto.RisqueBudget` sont omis (`null`) sans `FINANCIAL_DATA_VIEW`, projection faite dans le service (pas de garde au niveau contrôleur, ces endpoints portant aussi des données non financières — même pattern que `TimeEntryDto.FinancialSnapshot` au Lot 3). `ProjectParticipantService` réutilise `FinancialCalculationService.CalculateAsync` (Lot 2, non modifié) à la date du jour avec `HeuresSaisies = 0` pour exposer le TJM/société applicables sans dupliquer la recherche historisée.
- [x] `ProjectService.BuildFinancialSummaryAsync` agrège `TimeEntryFinancialSnapshot` par projet (via la navigation `TimeEntry.ProjectId`, une seule requête `GroupBy`) pour `CoutReelConsomme`/`CoutContractuelConsomme`/`Differentiel`/`BudgetRestant` ; méthode publique `GetCoutReelConsommeAsync` exposée séparément et réutilisée telle quelle par `ProjectPlanningService` pour `RisqueBudget`, afin d'éviter une deuxième implémentation de l'agrégation.
- [x] Conflits métier en 409 (`BusinessConflictException`) : chevauchement de périodes participant (`DateRangeOverlap`, partagé avec les Lots 2/3), version Initiale déjà existante, redondance Archiver/Réactiver un projet déjà dans le statut cible.
- [x] 5 contrôleurs REST sous `/api/v1` : `projects` (liste, détail, création, modification, `POST {id}/archive`, `POST {id}/reactivate` — pas de suppression physique, `CLAUDE.md` §7), `projects/{projectId}/participants` (liste, création, retrait = désactivation logique), `projects/{projectId}/plan-versions` + `.../weekly-plans` + `projects/{projectId}/planning` (synthèse §18.1/§29.5), `milestone-types`, `milestones` (filtres `projectId`/`responsableId`/`statut`/`enRetard`, ce dernier calculé en mémoire — champ dérivé non persisté).
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot4Seed.cs`) : 4 statuts de projet, 11 types de jalon, 2 projets (« Migration ELM » pleinement instrumenté — budget, participants, planning Initial et Ajusté Actif, date de fin ajustée postérieure à l'initiale pour démontrer `RisquePlanning = true` ; « Refonte VTOM » minimal), 2 participants, 1 version Initiale + 1 version Ajustée Active avec plans hebdomadaires, 3 jalons couvrant les statuts Terminé/EnCours-en retard/AVenir (démonstration d'`EstEnRetard`). La saisie de temps `TimeEntryLegrandProjet` du Lot 3 est désormais rattachée à « Migration ELM » (`Lot3Seed.cs` complété, pas dupliqué).
- [x] Migration `AddLot4Projects` générée pour les **trois** providers, y compris l'ajout de la colonne `project_id` (nullable) sur `time_entries` avec rétro-alimentation des 6 lignes seedées existantes. Appliquée et validée réellement sur SQLite (avertissement bénin de `dotnet ef` sur la reconstruction de table SQLite lors de l'ajout d'une contrainte FK à une table existante — comportement attendu du provider, migration terminée avec succès) ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — écart reconduit.
- [x] Tests : 21 nouveaux cas ajoutés (7 tests unitaires purs `ProjectPlanningCalculatorTests` sur les 3 fonctions de `ProjectPlanningCalculator` + 14 tests d'intégration `ProjectsTests` sur base SQLite réelle : synthèse financière avec/sans permission, participants avec TJM applicable, 409 chevauchement participant, synthèse de planning avec métriques attendues, `RisqueBudget` omis sans permission, 409 version Initiale déjà existante, **archivage automatique de la version Ajustée Active précédente lors de la création d'une nouvelle** (validation directe de la précision actée avec l'utilisateur), 400 version Ajustée sans motif, jalons filtrés par `enRetard`, archivage puis réactivation d'un projet + 409 sur une redondance). Un bug de données de test a été détecté et corrigé pendant l'écriture (`CalculateBudgetRisk_ComparesConsumedCostToInitialBudget`, ordre des paramètres des `[InlineData]` inversé par rapport à la signature — corrigé après un premier run de tests en échec). Aucune règle de planning testée sur repository mocké (`CLAUDE.md` §14). Suite complète : **80/80 réussis** (21 nouveaux + 59 des Lots 0-3).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète, 8 projets, `--no-incremental`) → 0 avertissement, 0 erreur ; `dotnet test` → 80/80 ; `npm run build` / `npm run lint` / `npm run format:check` (frontend, inchangé ce lot) → tous verts.

### Code — Lot 5 (2026-07-15)

- [x] **`Order`** complété (§13.2) : `ProjectId` (lien direct différé depuis le Lot 1), `BudgetFinancierAjuste`/`BudgetJoursAjuste`/`DateFinAjustee` (démarrent égaux à l'engagement initial, évoluent uniquement via une rallonge). **`OrderExtension`** (§13.3, rallonges) : entité append-only, applique atomiquement son montant/ses jours/sa nouvelle date de fin à la commande dans la même transaction (`OrderExtensionService`), bloquée sur commande Clôturée (409). Statuts de commande pilotés par une **vraie machine d'état** (précision actée avec l'utilisateur) : transitions dédiées (`activate`/`suspend`/`mark-consumed`/`close`), aucune ne permet de sortir de Clôturée — seule `reopen` (motif obligatoire) le permet.
- [x] **`Budget` + `BudgetVersion`** (§14) : enveloppe de suivi liée à un projet et/ou une commande (au moins l'un des deux, simplification documentée pour le « périmètre de pilotage » libre), consommé/reste/atterrissage/risque toujours calculés à la demande (`BudgetService`, jamais des colonnes). `BudgetVersion` historise chaque ajustement (ancienne/nouvelle valeur, motif, auteur, date, référence textuelle facultative). Ressource intégralement financière : `BudgetsController` entièrement gardé par `FINANCIAL_DATA_VIEW` (`RequirePermissionAttribute`, même principe que le Lot 2), aucune omission de champ à la pièce.
- [x] Écart Lot 4 résolu — **formule littérale du §29.5** : `ProjectPlanningCalculator.CalculateAtterrissageFinancier` (extrapolation proportionnelle sur le facteur de la charge, coût réel consommé si aucune charge consommée — formule MVP simple validée par l'utilisateur) et `CalculateBudgetRisk` mis à jour pour comparer l'atterrissage financier au **budget ajusté** (recherché via le `Budget` lié au projet, repli documenté sur `Project.BudgetInitial` si aucun `Budget` n'est créé) — remplace la comparaison simplifiée coût réel/budget initial du Lot 4. `BudgetService` réutilise cette même fonction pour son propre risque de dépassement (`CLAUDE.md` §5, pas de règle dupliquée).
- [x] **`ReportingService`** (`Application/Reporting/`), toutes les agrégations §21/§25/§26 : charges (§21, filtres complets, décomptes de références INC/CHG/PRB/RITM/VABE/VSR **distinctes** — pas des lignes de saisie), tableau de bord (§25, KPI opérationnels toujours renvoyés + KPI financiers gardés par permission), rapports opérationnel/financier (§26.1/§26.2). Réutilise `AvailabilityService` (Lot 3) pour la capacité théorique/réelle — jamais recalculée — et `ProjectPlanningCalculator` pour le risque budgétaire. **`ReportingPeriodResolver`** (fonction pure, `Jour`/`Semaine`/`Mois`/`Annee`/`Personnalisee` → bornes explicites) testée sans base de données (`CLAUDE.md` §14). Écart Lot 4 résolu — `GetProjectLinkedReferencesAsync` ferme le manque de synthèse des références liées à un projet (§17.7).
- [x] **`ExportService`** (§26.3) : génération réelle CSV (natif), Excel (**ClosedXML**) et PDF (**QuestPDF**, licence Community) à partir d'une représentation tabulaire générique (`ReportingTableDto`) produite exclusivement par `ReportingService` — `ExportService` ne fait que restituer, aucun calcul métier (demande explicite de l'utilisateur : tous les calculs restent dans `BudgetService`/`ReportingService`/`ProjectPlanningService`/`FinancialCalculationService`, jamais dans un contrôleur). Chaque export embarque ses métadonnées minimales (date de génération, auteur, version de l'application lue via `Assembly.GetName().Version`, filtres appliqués) et journalise une entrée **`ExportLog`** (entité légère et dédiée, volontairement distincte de l'`AuditLog` général du Lot 6).
- [x] **`DashboardKpi`** (§30, référentiel administrable, même principe que `MilestoneType`) avec `Category` (Opérationnel/Financier, demande explicite) — seule la définition est persistée, la valeur reste toujours calculée à la demande. Permission **`TIME_ENTRY_CORRECTION`** (demande explicite, §13.4) : `TimeEntryService` bloque désormais une saisie sur une commande Clôturée (409) sauf pour un appelant portant cette permission.
- [x] 4 nouveaux contrôleurs REST + extension d'`OrdersController` : `orders` (transitions de statut, `orders/{id}/extensions`), `budgets` (+ `versions`), `dashboard-kpis`, `reporting` (`/charges`, `/dashboard`, `/operational`, `/financial` gardé par permission, `/projects/{id}/linked-references`, `/charges/export` et `/financial/export` avec paramètre `format`).
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot5Seed.cs`) : permission `TIME_ENTRY_CORRECTION` accordée à Bernard, une rallonge historique sur la commande de démonstration (`Lot1Seed.cs` mis à jour pour refléter l'état post-rallonge : +20 000 €, +30 jours, échéance repoussée), un budget lié à « Migration ELM » avec un ajustement historisé (150 000 € → 180 000 €), 8 KPI de tableau de bord (6 opérationnels, 2 financiers).
- [x] Migration `AddLot5BudgetsAndReporting` générée pour les **trois** providers. Appliquée et validée réellement sur SQLite (même avertissement bénin de reconstruction de table SQLite qu'au Lot 4, migration terminée avec succès) ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — écart reconduit.
- [x] Tests : 25 nouveaux cas ajoutés (9 tests unitaires purs — 7 `ReportingPeriodResolverTests` + 2 `ProjectPlanningCalculatorTests` sur `CalculateAtterrissageFinancier` — + 16 tests d'intégration `BudgetsAndReportingTests` sur base SQLite réelle : machine d'état des commandes complète avec blocage de réouverture directe, rallonge et blocage sur commande clôturée, saisie de temps bloquée/autorisée selon `TIME_ENTRY_CORRECTION`, budget avec/sans permission (403), ajustement de budget historisé, clôture/réactivation de budget, charges filtrées avec décomptes de références distinctes, tableau de bord avec/sans KPI financiers, 403 sur le rapport financier, références liées à un projet, exports réels non vides dans les 3 formats (`[Theory]`), 403 sur l'export financier, KPI de tableau de bord seedés). Deux ajustements d'isolation de test ont été nécessaires en cours d'écriture (budget et commande de démonstration partagés par plusieurs tests de la même classe — voir « Décisions actées »). Aucune règle financière/reporting testée sur repository mocké (`CLAUDE.md` §14). Suite complète : **105/105 réussis** (25 nouveaux + 80 des Lots 0-4).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète, 8 projets, `--no-incremental`) → 0 avertissement, 0 erreur ; `dotnet test` → 105/105 ; `npm run build` / `npm run lint` / `npm run format:check` (frontend, inchangé ce lot) → tous verts.

### Code — Lot 6 (2026-07-17)

- [x] **`AuditLog`** (§28.3, entité obligatoire différée depuis le Lot 1) : append-only, jamais modifiable depuis l'interface standard (aucun endpoint d'écriture n'est exposé — seul `AuditService` écrit). `AuditService.RecordAsync` ajoute l'entrée au suivi EF Core sans appeler `SaveChangesAsync` : c'est le `SaveChangesAsync` du service métier appelant, exécuté juste après, qui persiste les deux ensembles de modifications dans une seule transaction — « même transaction que le changement métier » sans Unit of Work dédiée. Câblée dans `TimeEntryService` (création/modification/suppression logique), `TimeEntryRevaluationService` (recalcul), `UserService` (modification/désactivation/changement de rôle/permission), `ProjectService` (création/modification/archivage/réactivation), `MilestoneService` (création/modification), `SettingsService` (modification), `OrderService` (création/modification/transitions de statut), `OrderExtensionService` (rallonge), `OrderReceiptService` (réception), `CompanyService` (création/modification), `ResourceTjmHistoryService`/`CompanyContractHistoryService` (création/modification), `ExportService` (export financier uniquement), `ImportService` (import/comparaison d'import) — couvre l'intégralité de la liste minimale du §28.3. `AuditLogsController` (`GET /api/v1/audit-logs`, `GET /{id}`) gardé par la nouvelle permission `AUDIT_VIEW`.
- [x] **Écarts fermés du §28.3** qui exigeaient des endpoints inexistants avant ce lot (constat fait à l'ouverture du Lot 6, décision validée par l'utilisateur de les construire ici plutôt que de les redocumenter comme écart) : `UserService.UpdateAsync`/`DeactivateAsync`/`ReactivateAsync`/`ChangeRoleAsync`/`GrantPermissionAsync`/`RevokePermissionAsync` (nouveaux, gardés par `USER_ADMINISTRATION`, garde-fou CLAUDE.md §17 — impossible de retirer le dernier accès Administrateur actif, appliqué comme invariant système) ; `TimeEntryService.DeleteAsync` (suppression logique, `DELETE /api/v1/time-entries/{id}`) ; `ITimeEntryRevaluationService.RecalculateAsync` (Lot 3, `NotImplementedException`) enfin implémenté (`POST /api/v1/time-entries/{id}/recalculate`, gardé par la nouvelle permission dédiée `TIME_ENTRY_RECALCULATION`, motif obligatoire, ancienne valeur conservée dans `AuditLog.OldValue` — la relation 1-1 à clé partagée de `TimeEntryFinancialSnapshot` ne permet pas une seconde ligne de snapshot, l'historique de la valeur précédente vit donc dans l'audit, pas dans une nouvelle ligne). `CompanyService.UpdateAsync` (nouveau, `PUT /api/v1/companies/{id}`, Code volontairement non modifiable) ferme également l'absence de mise à jour du référentiel sociétés relevée par le §28.3 "modification d'une société".
- [x] **Nouvelles règles métier validées par l'utilisateur (Product Owner)** : (1) le vocabulaire « Demande d'achat → Commande → Réceptions partielles → Clôture » se représente par la machine d'état `Order`/`OrderStatus` **inchangée depuis le Lot 5** (aucune régression sur un lot déjà tagué `v0.5.0`), complétée par une nouvelle entité append-only **`OrderReceipt`** (réceptions partielles répétables, montant ou jours, jamais modifiée — une correction est une nouvelle réception, éventuellement négative ; blocage au-delà du reste réceptionnable sauf permission dédiée `ORDER_RECEIPT_OVERRIDE` ; bloquée sur commande Clôturée ; auditée). `OrderReceiptService`, `OrderReceiptsController` (`orders/{orderId}/receipts`, `+ /summary`). (2) La société d'une commande est immuable après création — déjà garanti structurellement (`OrderUpdateRequest` ne porte pas `CompanyId`), expliqué en commentaire. (3) La ressource affectée à une commande peut changer en cours de vie — déjà supporté (`AuthorizedResourceIds` remplaçable via `PUT /orders/{id}`), désormais audité. (4) Les références INC/CHG/PRB/RITM/VABE/VSR sont des références RUN, rattachables à un projet sans faire partie du modèle `Project` — déjà conforme à l'architecture (`TimeEntry.Reference`/`ProjectId`, jamais un champ sur `Project`), mais **incohérence de données corrigée** : `VABE`/`VSR` étaient seedés `IsRun: false` depuis le Lot 3 (`Lot3Seed.cs`), corrigé à `true` (migration Lot 6 : mise à jour des deux lignes seedées, même mécanisme que la rétro-alimentation actée au Lot 4).
- [x] **Moteur d'import générique** (§27, `Application/Imports/`) : pipeline unique Aperçu (`Preview`, aucune écriture) → Simulation (`SimulateAsync`, aucune écriture, §27.3 étapes 5-9) → Exécution (`ExecuteAsync`, écrit `ImportBatch`/`ImportDiff`, audite, §27.3 étapes 10-12), partagé par les **16 types importables** du §27.1 via un adaptateur dédié par type (`IImportAdapter`, réutilise exclusivement les services métier existants — aucun accès direct au contexte EF Core pour les entités importées, règle validée à l'ouverture du lot). `CsvRequestBinder` (liaison ligne CSV → DTO par réflexion générique, une seule implémentation) et `FieldDiffer` (diff de mise à jour par réflexion générique) partagés par tous les adaptateurs. `CsvFileParser` (détection encodage/séparateur, §27.3 étape 4) est une fonction pure testée sans base de données.
- [x] **Modes par type, strictement dérivés de ce que le service métier sous-jacent expose déjà** (aucune méthode de service créée uniquement pour l'import) : Ajout seul pour `Resources`, `Applications`, `Organisation` (Department/Service/Team en un seul type, colonne `Niveau`), `Absences`, `ProjectParticipants` (+ pas de Complet : scopé à un projet, le moteur n'a pas de notion de « périmètre de fichier ») ; Ajout + Mise à jour pour `Companies`, `ResourceTjmHistories`, `CompanyContractHistories`, `ResourceCompanyAssignments` (historisées, Complet non applicable), `Orders` (machine d'état non compatible avec un archivage déduit d'un import), `Milestones`, `Plannings` (upsert par ligne via `ProjectPlanningService.SetWeeklyPlansAsync`, appelé avec une liste d'un élément par ligne pour conserver la granularité d'erreur/diff par ligne) ; Ajout + Mise à jour + Complet pour `Projects` (archivage via `ProjectService.ArchiveAsync`), `Budgets` (clôture via `BudgetService.CloseAsync`), `Users` (désactivation), `TimeEntries` (suppression logique). Import d'un type financier (TJM, contrats, rattachements société, budgets, commandes) exige en plus `FINANCIAL_DATA_VIEW`, contrôlé au niveau contrôleur (`ImportsController`, gardé par `IMPORT_EXECUTE`).
- [x] **Import SharePoint simulé** (§27.4) : même pipeline, `source = "SharePoint"` (`POST /api/v1/imports/sharepoint/execute`). « Comparer au chargement précédent » = dernier `ImportBatch` confirmé du même type/de la même source (`ImportBatch.PreviousBatchId`), diffé contre l'état actuel de la base plutôt qu'un fichier précédent stocké — décision validée par l'utilisateur, architecture ouverte à un futur connecteur réel (point d'extension : l'obtention des octets du fichier).
- [x] 3 nouveaux contrôleurs REST + extensions : `ImportsController` (`types`, `preview`, `simulate`, `execute`, `sharepoint/execute`, historique des lots + diffs), `AuditLogsController` (lecture seule, gardé par `AUDIT_VIEW`), `OrderReceiptsController` (sous-ressource de `orders`) ; `UsersController` étendu (`PUT`, `deactivate`, `reactivate`, `PUT /role`, `POST`/`DELETE /permissions/{code}`) ; `TimeEntriesController` étendu (`DELETE`, `POST /recalculate`) ; `CompaniesController` étendu (`PUT`).
- [x] 5 nouvelles permissions (§6.2) : `USER_ADMINISTRATION`, `TIME_ENTRY_RECALCULATION`, `IMPORT_EXECUTE`, `AUDIT_VIEW`, `ORDER_RECEIPT_OVERRIDE` — toutes accordées à Bernard (seul Administrateur du jeu de données) dans le seed de démonstration.
- [x] `IRepository<T>.RemoveAsync` ajouté (Lot 6) : seul cas d'usage à ce jour, le retrait d'une permission utilisateur (`UserPermission`, clé composite, relation de rattachement pur sans portée historique — jamais utilisé pour une entité couverte par « statut plutôt que suppression physique »).
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot6Seed.cs`) : les 5 nouvelles permissions accordées à Bernard, une réception partielle sur la commande de démonstration, un lot d'import confirmé illustratif (Applications, Ajout, un ajout + une modification), deux entrées d'audit illustratives.
- [x] Migration `AddLot6ImportsAndAudit` générée pour les **trois** providers (y compris la correction de données `VABE`/`VSR.IsRun`). Appliquée et validée réellement sur SQLite via les tests d'intégration (`WebApplicationFactory`) ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — écart reconduit depuis le Lot 0.
- [x] Tests : 43 nouveaux cas ajoutés (17 tests unitaires purs — `CsvFileParserTests` (7), `CsvRequestBinderTests` (6), `FieldDifferTests` (4), aucun n'accède à une base de données — + 26 tests d'intégration sur base SQLite réelle : `Lot6SecurityAndOrdersTests` (16 — administration utilisateur incluant le garde-fou dernier administrateur et l'audit de promotion/retrait Administrateur, suppression logique et recalcul d'une saisie avec motif obligatoire, réceptions de commande avec/sans dépassement autorisé, modification de société, 403 sans permission dédiée) et `Lot6ImportsTests` + `ImportCompleteModeTests` (10 — cycle aperçu/simulation/exécution sans effet de bord de la simulation, ajout et mise à jour avec diff de champ, mode non supporté → 409, permission financière requise en plus d'`IMPORT_EXECUTE`, comparaison SharePoint entre deux lots, mode Complet isolé dans sa propre base pour ne pas archiver globalement des entités utilisées par d'autres tests — convention actée au Lot 5). Suite complète : **148/148 réussis** (43 nouveaux + 105 des Lots 0-5).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète, `--no-incremental`) → 0 avertissement, 0 erreur ; `dotnet test` → 148/148 ; `npm run build` / `npm run lint` / `npm run format:check` (frontend, inchangé ce lot) → tous verts.

### Code — Lot 7 (2026-07-17)

Premier lot de la nouvelle phase « Frontend complet (V1) » (révision de roadmap actée à la clôture du Lot 6 — voir `docs/ROADMAP.md`). Construit exclusivement les fondations frontend réutilisées par tous les lots suivants ; **aucun écran métier** (Temps, Projets, Commandes, Budgets, Dashboard...) n'est développé ce lot, conformément au périmètre validé.

- [x] **Seul ajout backend du lot** : `GET /api/v1/permissions` (+ `GET /{id}`), référentiel en lecture seule (`PermissionDto`, `PermissionService`, `PermissionsController`, aucune permission requise — même principe d'ouverture qu'`ActivityTypesController`). Nécessaire car `UserDto.PermissionIds` (Lot 1) n'expose que des GUIDs : c'est le seul point de résolution GUID → Code permettant au frontend d'évaluer une garde de permission. Aucune autre modification backend ce lot.
- [x] **Identité visuelle** (`src/theme/theme.ts`, cahier des charges §8.1) : thème MUI dédié — fond gris clair (`#F4F5F7`), sidebar bleu marine (`#0B1F3D`), cartes blanches, accent bleu Safran (`#0055B8`), typographie Inter Variable (self-hébergée via `@fontsource-variable/inter`, alignée sur la contrainte offline future du profil PortableDemo) avec repli Segoe UI, `MuiButton`/`MuiCard`/`MuiPaper`/`MuiAppBar` personnalisés (élévation plate, pas d'effet gadget). Locales françaises MUI (`frFR`) appliquées au core, à `x-data-grid` et à `x-date-pickers`.
- [x] **Authentification de démonstration côté frontend** (`src/auth/`) : `DemoIdentityContext`/`DemoIdentityProvider` (identifiant choisi persisté en `localStorage`, préférence non sensible — CLAUDE.md §9), intercepteur de requête unique sur `apiClient` (`src/api/client.ts`) qui rejoue l'en-tête `X-Demo-User` existant (Lot 2, `DemoCurrentUserProvider`) — aucune nouvelle notion d'authentification introduite. `useCurrentUser` (TanStack Query) joint `UserDto.permissionIds` au référentiel `/api/v1/permissions` pour résoudre les codes de permission courants. `PermissionGuard` n'effectue **aucune** vérification serveur : il reflète uniquement la présence/absence de données déjà filtrées par l'API (CLAUDE.md §17, `docs/ARCHITECTURE.md` §3) — jamais une barrière de sécurité côté client.
- [x] **Filtrage réel de la sidebar par droits (§8.2), limité aux deux entrées où une garde serveur non ambiguë existe** : `Budgets` (`BudgetsController`, gardé au niveau classe par `FINANCIAL_DATA_VIEW` depuis le Lot 5) et `Imports` (`ImportsController`, gardé au niveau classe par `IMPORT_EXECUTE` depuis le Lot 6). Les 14 autres entrées de navigation restent toujours visibles : aucune garde de classe équivalente n'existe encore pour elles côté API (ex. `AuditLogsController` est gardé par `AUDIT_VIEW` mais n'est pas lui-même une entrée de sidebar — Audit devient un onglet d'Administration au Lot 8) ; inventer un filtrage non appliqué par le serveur aurait été trompeur.
- [x] **`AppLayout`** (`src/components/ui/AppLayout.tsx`) : assemble `Sidebar` (16 entrées §8.2, icônes `lucide-react`, source unique `src/routes/navigation.ts` partagée par la sidebar et le routeur), `Header` (sélecteur d'identité de démonstration), `Breadcrumb` (`useMatches()` + `route.handle.crumb`, masqué s'il n'y a qu'un seul niveau — respecte littéralement le §8.3 « fil d'Ariane sur les pages de détail », pas sur les listes) et la zone de contenu (`Outlet`).
- [x] **Composants transverses réutilisables** (`src/components/ui/`) : `DataTable` (wrapper `@mui/x-data-grid`, pagination et tri **toujours serveur**, conversion 1-based ↔ 0-based avec `PaginationQuery`/`PagedResult<T>`, CLAUDE.md §12), `FormField`/`FormSelect`/`FormDatePicker` (React Hook Form + Controller, erreurs affichées depuis `fieldState.error` — le schéma Zod reste défini par chaque écran), `Modal`, `ConfirmDialog`, `EmptyState` (état vide et écran « à venir »), `FilterBar`, `KpiCard`, `ProgressBar`, `StatusBadge` (délibérément agnostique du domaine : label + tonalité, jamais un enum métier codé en dur), `FinancialValue` (rendu basé sur la présence/absence de la valeur reçue, **jamais** sur une vérification de permission — CLAUDE.md §13).
- [x] **Routage complet** (`src/routes/router.tsx`) : une route par entrée de navigation (16 routes), générées depuis `navigation.ts` pour ne jamais diverger de la sidebar, chacune rendant `PlaceholderPage` (`EmptyState`, « Cet écran sera développé dans un lot ultérieur »). `RouteErrorBoundary` (`errorElement`) capture toute exception de rendu sans faire disparaître l'application entière (§8.3 « messages d'erreur compréhensibles »).
- [x] **Gestion centralisée des erreurs et du chargement** : `QueryClient` unique (`retry: 1`, pas de `refetchOnWindowFocus`) ; `getErrorMessage` (`src/api/errors.ts`) traduit une erreur Axios/`ProblemDetails` (y compris les erreurs de validation FluentValidation) en message affichable, point unique réutilisé par tous les écrans futurs plutôt qu'une lecture ad hoc d'`error.message`.
- [x] **Formats français** : `Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR' })` dans `FinancialValue` ; `FormDatePicker` affiche `DD/MM/YYYY` (`dayjs`, locale `fr`) tout en conservant `YYYY-MM-DD` dans l'état du formulaire, identique au format `DateOnly` de l'API (CLAUDE.md §7 : la conversion d'affichage est une responsabilité du frontend).
- [x] **Dépendances ajoutées** (aucune retirée) : `@mui/material`, `@emotion/react`, `@emotion/styled`, `@mui/x-data-grid`, `@mui/x-date-pickers`, `dayjs`, `lucide-react`, `@fontsource-variable/inter` ; outillage de test enfin installé (annoncé mais jamais posé depuis le Lot 0) : `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`. Script `npm run test` (`vitest run`) ajouté.
- [x] **Incompatibilité MUI v9 découverte et corrigée en cours de lot** : `Stack`/`Typography` (MUI v9.2.0) n'acceptent plus de props CSS raccourcies non déclarées explicitement dans leur `OwnProps` (`alignItems`, `justifyContent`, `flexWrap`, `fontWeight`, `fontSize` sur `ListItemText.slotProps.primary`) — contrairement aux versions précédentes de MUI qui les acceptaient en pass-through. Ce n'était pas un problème TypeScript/outillage mais un changement d'API réel de la bibliothèque ; toutes ces props sont désormais passées via `sx`, conformément à la documentation MUI v9.
- [x] Tests frontend (Vitest + React Testing Library), **43 cas, 0 régression** : `errors.test.ts` (6, extraction de message ProblemDetails/validation/réseau), `demoIdentityStorage.test.ts` (3), composants (`StatusBadge`, `FinancialValue`, `EmptyState`, `ProgressBar`, `KpiCard`, `Modal`, `ConfirmDialog`, `FilterBar`, `DataTable`, `Breadcrumb`, `Sidebar` — filtrage par permission vérifié avec/sans identité choisie, mocks des endpoints `/users`/`/permissions`), `useCurrentUser.test.tsx` et `PermissionGuard.test.tsx` (résolution GUID → code, mise à jour réactive au changement d'identité). Cleanup RTL déclaré explicitement dans `src/test/setup.ts` (`globals: false` conservé, imports explicites — CLAUDE.md §9 — donc l'auto-cleanup implicite de `@testing-library/react` ne s'active pas).
- [x] Tests backend : 2 nouveaux cas dans `ReferentialsTests.cs` (liste des permissions seedées, 404 sur GUID inconnu). Suite complète : **150/150 réussis** (2 nouveaux + 148 des Lots 0-6).
- [x] Vérification manuelle en navigateur (Playwright, Chromium) : API lancée en Development/SQLite, migrations appliquées (`dotnet ef database update`), frontend en `npm run dev` — sélecteur d'identité peuplé par les 13 utilisateurs seedés, `Budgets`/`Imports` absents sans identité puis visibles après sélection d'un utilisateur portant les permissions correspondantes, navigation « Projets » fonctionnelle avec écran « à venir », aucune erreur console/page. Captures conservées dans le dépôt (`docs/screenshots/lot-7/`, voir « Validation visuelle — Lot 7 » ci-dessous).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 150/150 ; `npm run build` → succès (avertissement non bloquant : bundle JS ~626 Ko avant gzip, MUI + DataGrid + DatePickers — le découpage en chunks est explicitement hors périmètre, relève de l'optimisation du Lot 13) ; `npm run test` → 43/43 ; `npm run lint` → 0 erreur ; `npm run format:check` → conforme.

#### Validation visuelle — Lot 7

Captures Playwright/Chromium prises en conditions réelles (API Development/SQLite + frontend `npm run dev`), conservées dans `docs/screenshots/lot-7/` :

| Capture | Contenu |
|---|---|
| `01-layout-sans-identite.png` | Layout initial (sidebar bleu marine, fond gris clair, header) avant sélection d'une identité de démonstration — `Budgets` et `Imports` absents de la sidebar, faute de permission résolue. |
| `02-selecteur-identite.png` | Sélecteur d'identité (Header) ouvert, peuplé par les 13 utilisateurs seedés (`GET /api/v1/users`). |
| `03-sidebar-permissions.png` | Après sélection d'Alexandre BERNARD (`s636140`, permissions financières/imports) : `Budgets` et `Imports` apparaissent dans la sidebar — `PermissionGuard` vérifié en conditions réelles contre l'API, pas seulement en test unitaire mocké. |
| `04-navigation-ecran-a-venir.png` | Navigation vers « Projets » : route fonctionnelle, entrée active mise en évidence dans la sidebar, écran neutre « à venir » (`PlaceholderPage`/`EmptyState`), conformément au périmètre du lot (aucun écran métier). |

### Code — Lot 8 (2026-07-20)

Premier lot de la phase « Frontend complet (V1) » à livrer des écrans métier réels (fiches Ressource/Société/Application, panneau Administration), sur la base des Lots 1-7. Périmètre révisé à l'ouverture du lot avec l'utilisateur (voir `docs/ROADMAP.md`) : 5 référentiels supplémentaires, absents du cahier des charges et de `docs/BACKLOG_METIER.md` avant ce lot, ont été validés puis implémentés — exception explicite à la règle « aucune évolution backend » de cette phase, pas une dérive de périmètre.

- [x] **5 nouveaux référentiels backend** (`docs/BACKLOG_METIER.md` §5-9, validés avec l'utilisateur avant implémentation, CLAUDE.md §5) : `Technology` (+ `ApplicationTechnology`/`ResourceTechnology`, liaisons many-to-many), `Client` (+ `Project.ClientId` nullable), `ProjectType` (+ `Project.ProjectTypeId` nullable), `CostCenter` (rattaché à `Department`/`Service`), `Currency` (référentiel de consultation, aucun impact sur `FinancialCalculationService`). Pattern identique aux référentiels existants (`AuditableEntity`, `ReferentialStatus`, `IEntityTypeConfiguration<T>`, création/modification auditées via `AuditService`).
- [x] **Liaisons Technology gérées par suppression/recréation explicite des lignes de jointure** (`TechnologyService.UpdateAsync`), pas par réaffectation de la collection de navigation : `IRepository<T>.Query()` est non suivi (`AsNoTracking`, CLAUDE.md §11), une entité récupérée ainsi puis mutée ne serait jamais persistée. Même principe que `UserService.RevokePermissionAsync` (suppression par « stub » ne portant que la clé).
- [x] 5 nouveaux contrôleurs REST sous `/api/v1` : `technologies` (+ filtres `applicationId`/`resourceId`), `clients`, `project-types`, `cost-centers`, `currencies` — CRUD complet (liste/détail/création/modification), aucune permission requise (référentiels non financiers, même principe d'ouverture que `departments`/`activity-types`).
- [x] Migration `AddLot8ReferentialsAndAdministration` générée pour les **trois** providers. Appliquée et validée réellement sur SQLite (`dotnet ef database update`, avertissement bénin de reconstruction de table SQLite déjà rencontré aux Lots 4/5/6) ; PostgreSQL et SQL Server générés et compilables (design-time), non testés contre une instance réelle — écart reconduit depuis le Lot 0.
- [x] Seed de démonstration idempotent (`Infrastructure/Persistence/Seed/Lot8Seed.cs`) : 3 technologies (2 rattachées à IBM ELM, 1 ressource avec 2 technologies), 2 clients, 3 types de projet, 2 centres de coûts rattachés à l'organisation du Lot 1, 2 devises (EUR/USD). `ProjectMigrationElm` (Lot 4) complétée avec un type de projet et un client (`Lot4Seed.cs` mis à jour, pas dupliqué — même convention qu'au Lot 4 pour `TimeEntryLegrandProjet`).
- [x] Tests backend : 17 nouveaux cas (`Lot8ReferentialsTests.cs`, intégration sur base SQLite réelle) — liste seedée par référentiel, filtrage Technology par `applicationId`, création valide/invalide (code vide, département inconnu, code ISO invalide), modification auditée avec conversion de statut, **remplacement des liaisons Application d'une Technology sans ligne orpheline** (vérifie explicitement le point ci-dessus), `ProjectDto` expose bien `ProjectTypeId`/`ClientId` pour le projet seedé. Suite complète : **167/167 réussis** (17 nouveaux + 150 des Lots 0-7).
- [x] **Fiche Ressource/Utilisateur à 4 sections** (`/ressources/:id`) : Général, Organisation (département/service/équipe résolus par libellé), Sécurité (compte utilisateur lié le cas échéant — identifiant, rôle, permissions accordées — trouvé en joignant `UserDto.resourceId`), Historique des TJM sous `PermissionGuard code="FINANCIAL_DATA_VIEW"` (adaptation d'affichage uniquement, la garde réelle reste le 403 serveur du Lot 2, CLAUDE.md §17).
- [x] **Fiche Société** (`/societes/:id`) : informations générales + historique des contrats (confidentiel, §12.4), même principe de `PermissionGuard` que ci-dessus.
- [x] **Fiche Application** (`/applications/:id`) : informations générales + **détail statistique réutilisant `GET /api/v1/reporting/charges` du Lot 5** filtré par `applicationId` (charge totale/RUN/hors RUN, décomptes INC/CHG/PRB/RITM) — aucune nouvelle agrégation backend, fermeture de l'écart noté au Lot 1 (« aucun endpoint d'agrégation dédié à une fiche application ») en réutilisant l'endpoint générique déjà livré — + technologies rattachées (Lot 8).
- [x] **Panneau Administration** (`/administration`, `AdministrationPage.tsx`) : 13 onglets d'origine + 5 nouveaux référentiels, assemblés via une coquille partagée `ReferentialAdminTab` (liste paginée serveur + dialog de création/édition, CLAUDE.md §9 — pas de duplication de la logique de liste/dialog entre onglets, mais un formulaire dédié par référentiel, les champs différant réellement d'un domaine à l'autre). Chaque onglet expose **exactement** ce que l'API supporte, jamais plus : `Département`/`Services`/`Équipes`/`Applications`/`Types d'activités`/`Types de jalons` = liste + création (l'API ne les expose pas en modification) ; `Sociétés`/`Technologies`/`Clients`/`Types de projet`/`Centres de coûts`/`Devises` = liste + création + modification ; `Utilisateurs` = liste + activer/désactiver (`USER_ADMINISTRATION`) ; `Commandes` = liste de consultation seule (la gestion complète relève du Lot 11, non anticipée) ; `Paramètres` = formulaire singleton GET/PUT ; `Permissions`/`Audit` = liste seule (`Audit` gardé par `PermissionGuard code="AUDIT_VIEW"`) ; `Types d'absences` = liste fixe non administrable (`AbsenceType` est un enum C#, pas une entité, Lot 3).
- [x] Client API frontend étendu : `src/api/types.ts` (DTO de tous les référentiels touchés), 15 nouveaux modules `src/api/endpoints/*.ts` (un par domaine, même convention que le Lot 7), `src/api/endpoints/reporting.ts` (réutilisation de `/reporting/charges`).
- [x] `DataTable` (Lot 7) étendu d'une prop optionnelle `onRowClick` (absente par défaut, rétrocompatible) — nécessaire pour la navigation liste → fiche détail et pour l'édition par clic de ligne dans `ReferentialAdminTab`.
- [x] Dépendance ajoutée : `@hookform/resolvers` (glue officielle React Hook Form ↔ Zod, CLAUDE.md §9 — les deux étaient déjà dans la stack sans jamais avoir été reliés, aucun formulaire métier n'existant avant ce lot).
- [x] Tests frontend (Vitest + RTL), **12 nouveaux cas, 0 régression** : `DataTable` (clic de ligne avec/sans handler), `ReferentialAdminTab` (liste, absence de bouton Créer sans `renderCreateForm`, cycle création → rafraîchissement, ouverture d'édition au clic de ligne), `ClientForm` (rejet si champs obligatoires vides, soumission réussie, **conversion correcte du statut string↔enum via `FormSelect`**), `AbsenceTypesTab` (liste fixe sans action de création), `CompanyDetailPage` (historique des contrats masqué/affiché selon `FINANCIAL_DATA_VIEW`, avec mock de `useCurrentUser`). Suite complète : **55/55 réussis** (12 nouveaux + 43 des Lots 0-7).
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 167/167 ; `npm run build` → succès (même avertissement non bloquant qu'au Lot 7, bundle ~1,3 Mo avant gzip — inchangé en cause, découpage en chunks toujours hors périmètre, Lot 13) ; `npx tsc -b` → 0 erreur ; `npm run test` → 55/55 ; `npm run lint` → 0 erreur.

#### Validation visuelle — Lot 8 : exception de gouvernance actée au Lot 14

**Constat (audit d'architecture, Lot 14)** : contrairement à tous les autres lots frontend (7, 9 à 13), le Lot 8 n'a jamais eu de section « Validation visuelle » ni de dossier `docs/screenshots/lot-8/`, alors que `CLAUDE.md` §20 l'exige explicitement pour la clôture d'un lot frontend. Le lot avait néanmoins été vérifié par build/tests automatisés réels (167/167, voir ci-dessus) au moment de sa clôture.

**Décision actée** : ne pas fabriquer rétroactivement des captures présentées comme celles du Lot 8 — l'état actuel de ces écrans (fiches Ressource/Société/Application, panneau Administration) a évolué depuis via des composants partagés retouchés par des lots ultérieurs (`DataTable`, `PermissionGuard`, `ReferentialAdminTab`), des captures prises aujourd'hui ne représenteraient pas fidèlement l'état livré au Lot 8. Ce gap reste donc explicitement documenté comme un écart de gouvernance non corrigé plutôt que masqué par une preuve non authentique — cohérent avec le principe CLAUDE.md §3 de ne jamais donner une preuve de validation trompeuse.

### Code — Lot 9 (2026-07-20)

Deuxième lot de la phase « Frontend complet (V1) » à livrer des écrans métier réels : Saisie des temps, Mes absences (workflow complet), Disponibilités (capacité/charge/calendrier coloré) — construits exclusivement sur l'API des Lots 1-8, réutilisée sans duplication de calcul (`CLAUDE.md` §5). Deux évolutions backend ponctuelles, validées individuellement avec l'utilisateur avant implémentation et consignées dans `docs/BACKLOG_METIER.md` §10/§12 — exception de gouvernance documentée (`docs/BACKLOG_METIER.md`, « Exception de gouvernance — Lot 9 »), de portée nettement plus restreinte que celle du Lot 8 (aucune nouvelle entité, aucune migration).

- [x] **`GET /api/v1/time-entries` étendu** (`docs/BACKLOG_METIER.md` §10) : trois nouveaux paramètres optionnels `activityTypeId`/`projectId`/`orderId` sur l'action `GetList` déjà existante de `TimeEntriesController`/`TimeEntryService` — filtrage sur des colonnes déjà présentes depuis les Lots 3/4, aucune nouvelle entité, aucune migration, aucune nouvelle règle de calcul.
- [x] **`PUT /api/v1/absences/{id}` ajouté** (`docs/BACKLOG_METIER.md` §12) : nouvelle action `AbsenceService.UpdateAsync`, restreinte au statut Brouillon (409 sur tout autre statut, même principe que les autres transitions du service) — ferme l'exigence §23.2 « modifier tant que permis », jusque-là non couverte (seuls `Create`/`Submit`/`Validate`/`Refuse`/`Cancel` existaient).
- [x] **Écart backend documenté, volontairement non corrigé** (`docs/BACKLOG_METIER.md` §11, 🔎 À clarifier) : `TimeEntry` ne porte aucun `ApplicationId` (§19.1), antérieur au Lot 9 (introduit au Lot 3, jamais documenté avant cette analyse) — le formulaire Saisie des temps n'expose donc pas de sélecteur Application ni de filtre « par application », décision actée (Option A, ne pas étendre le modèle ce lot).
- [x] **Écran Saisie des temps** (`/temps`, `TimeEntriesPage.tsx`) : couverture complète des filtres du §19.4 — ressource, type d'activité, **projet et commande** (câblés sur les paramètres `projectId`/`orderId` ajoutés côté backend ce lot), période (Du/Au), et **semaine** (`TextField type="date"` « Semaine (jour contenu) » qui calcule les bornes lundi-dimanche via `weekBounds`, `lib/dateUtils.ts`, et les applique à Du/Au — aucune nouvelle règle de calcul, la même fonction est partagée avec `/disponibilites`). **Totalisation automatique** : `KpiCard` « Total (filtre courant) » sommant `dureeHeures` sur l'ensemble des saisies correspondant au filtre (requête dédiée `pageSize=1000` sur `GET /time-entries` déjà existant, pas la page affichée seule, aucun endpoint ajouté — voir « Décisions actées »). `DataTable` avec sous-objet financier (`FinancialValue`, omis sans `FINANCIAL_DATA_VIEW` — jamais recalculé côté frontend), actions Dupliquer/Recalculer (gardée par `PermissionGuard code="TIME_ENTRY_RECALCULATION"`, motif obligatoire, réutilise `POST /time-entries/{id}/recalculate` du Lot 6)/Supprimer (suppression logique, Lot 6). Formulaire de création/modification (`TimeEntryForm.tsx`) : validation de référence entièrement pilotée par les métadonnées `ActivityType.ReferenceRequired`/`ReferenceFormatRegex`/`ReferenceExample` (Lot 3) — la même donnée est simplement relue côté client, jamais une seconde règle. Ressource par défaut = ressource de l'identité de démonstration courante, sélecteur toujours disponible (aucune restriction non portée par le backend). Seul point du §19.4 hors périmètre : « filtrer par application » (`docs/BACKLOG_METIER.md` §11, voir ci-dessous).
- [x] **Écran Mes absences** (`/mes-absences`, `AbsencesPage.tsx`) : KPI « Jours validés (mois) »/« (année) »/« Taux de disponibilité (mois) » calculés exclusivement via `GET /resources/{id}/availability` (`AvailabilityService`, Lot 3, `JoursAbsenceValidee`) — jamais sommés côté frontend à partir des plages de dates brutes (aurait compté les week-ends à tort). Workflow complet piloté par le statut de la ligne (icônes conditionnelles : Modifier/Soumettre sur Brouillon via le nouveau `PUT`, Valider/Refuser sur Soumis avec motif de refus optionnel, Annuler sinon) — aucun nouveau statut, aucune nouvelle règle, réutilisation intégrale de `AbsenceService` (Lot 3).
- [x] **Écran Disponibilités** (`/disponibilites`, `AvailabilityPage.tsx`) : filtres département/service/équipe + bascule période Mensuelle/Hebdomadaire (`periodBounds`, fonction pure ; les bornes hebdomadaires délèguent à `weekBounds` (`lib/dateUtils.ts`), partagée avec le filtre « Semaine » de `/temps` — `.locale('fr')` explicite pour des semaines lundi-dimanche, l'application ne fixe jamais `dayjs.locale('fr')` globalement, seulement `LocalizationProvider adapterLocale="fr"` pour les DatePickers MUI). Tableau « Capacité par ressource » : un appel `GET /resources/{id}/availability` par ressource (`useQueries`), N+1 accepté pour ce lot (décision actée, cf. « Décisions actées » et « Écarts connus » ci-dessous). « Calendrier coloré » (§22.2) : composition de faits déjà exposés (jours fériés, absences validées, week-ends calculés) en jours Disponible/Férié/Absence/Week-end — pure présentation, aucune nouvelle agrégation backend. Point du §22.2/§29.6 non couvert : « disponibilités restantes » et « surcharges » (voir « Écarts connus » ci-dessous).
- [x] Client API frontend étendu : `src/api/types.ts` (DTO `TimeEntryDto`/`AbsenceDto`/`AvailabilityResultDto`/`HolidayCalendarDto`/`ProjectDto`, miroir exact des DTO backend), 5 nouveaux modules `src/api/endpoints/*.ts` (`timeEntries`, `absences`, `availability`, `holidayCalendar`, `projects`), même convention que les Lots 7/8.
- [x] Tests backend : 5 nouveaux cas dans `TimeAndCapacityTests.cs` (intégration, base SQLite réelle partagée par la classe) — filtrage par `activityTypeId`/`projectId`/`orderId` (le cas `orderId` réutilise l'entrée déjà seedée `TimeEntryBernardIncident`/`OrderDemo` plutôt que d'en créer une nouvelle, pour ne pas perturber un autre test « premier résultat » de la même classe, `CLAUDE.md` §14), modification d'un Brouillon (`PUT`), 409 sur tentative de modification après soumission. Suite complète : **172/172 réussis** (5 nouveaux + 167 des Lots 0-8).
- [x] Tests frontend (Vitest + RTL), **30 nouveaux cas, 0 régression** : `validateReference` (5 cas — même métadonnée relue côté client, jamais une seconde règle), `TimeEntryCreateForm` (blocage client si référence obligatoire manquante), `AbsenceCreateForm`/`AbsenceEditForm` (création, conversion du statut/type string→enum via `FormSelect`, même pattern que le Lot 8), `periodBounds` (2 cas — bornes lundi-dimanche vérifiées explicitement). `FormDatePicker` MUI (sections segmentées, non pilotable de façon fiable par `userEvent.type`) contourné par le mécanisme `seed` existant du formulaire de saisie ou, à défaut, un mock `TextField`+`Controller` local au fichier de test. **Tests d'interaction au niveau écran** (`TimeEntriesPage.test.tsx` 8 cas, `AbsencesPage.test.tsx` 8 cas, `AvailabilityPage.test.tsx` +4 cas) couvrant affichage, filtres (y compris projet/commande/semaine), création, modification, workflow d'absence complet (Soumettre/Valider/Refuser/Annuler), recalcul gardé par permission (vérifié à la fois présent et absent selon la permission de l'identité courante), et changement de ressource sur le calendrier de disponibilité — `TimeEntryForm`/`AbsenceForm` mockés dans ces tests de page pour isoler le câblage de l'écran (ouverture/fermeture de `Modal`, rafraîchissement de liste, propagation de la ligne dupliquée/éditée) de la logique de formulaire déjà couverte par `TimeEntryForm.test.tsx`/`AbsenceForm.test.tsx`. Suite complète : **85/85 réussis** (30 nouveaux + 55 des Lots 0-8).
- [x] **Commentaire XML mal placé, corrigé** (`AbsenceService.cs`) : l'insertion de `UpdateAsync` avant `SubmitAsync` avait laissé le `<summary>` documentant `SubmitAsync` au-dessus de `UpdateAsync` (deux balises `<summary>` empilées, `SubmitAsync` sans commentaire) — chaque méthode porte désormais son propre commentaire, aucun changement de comportement.
- [x] **`npm run format:check` corrigé sur le périmètre du lot** (revue post-clôture) : la première vérification de clôture avait déclaré ce contrôle conforme sans l'avoir réellement exécuté sur l'ensemble du dépôt — en le relançant, les 10 fichiers du Lot 9 (`TimeEntriesPage`/`TimeEntryForm`/`AbsencesPage`/`AbsenceForm`/`AvailabilityPage`, chacun avec son test) n'étaient en réalité pas conformes (lignes dépassant `printWidth: 100` non repliées par Prettier). Corrigé par `prettier --write` scopé à ces 10 fichiers uniquement (repli de ligne pur, aucun changement de comportement, diff vérifié sans différence hors espaces). Les **105 autres fichiers non conformes** détectés par `npm run format:check` sur l'ensemble du dépôt sont antérieurs au Lot 9 (Lots 7-8, ex. `AdministrationPage.tsx`, `ResourcesListPage.tsx`) — dérive de formatage préexistante, non introduite ni corrigée par ce lot (hors périmètre, à trancher séparément si un nettoyage global est souhaité).
- [x] Vérification manuelle en navigateur (Playwright, Chromium) : API lancée en Development/SQLite (migrations déjà appliquées depuis le Lot 8, aucune nouvelle migration ce lot), frontend en `npm run dev` — identité de démonstration Alexandre BERNARD (`s636140`, `FINANCIAL_DATA_VIEW`/`TIME_ENTRY_RECALCULATION`) sélectionnée, les 3 écrans visités avec données réelles (saisies valorisées avec coût réel affiché, absence validée avec KPI à 0 pour la période courante — cohérent, l'absence seedée est hors période, capacité/calendrier coloré corrects avec week-ends non coloriés), aucune erreur console/page. Captures conservées dans le dépôt (`docs/screenshots/lot-9/`, voir « Validation visuelle — Lot 9 » ci-dessous). Non rejouée lors de la revue post-clôture (accès navigateur non disponible cette session) : API (`/health`) et frontend (`npm run dev`, port 5173) redémarrés et vérifiés opérationnels, mais sans nouvelle capture — les écrans n'ont reçu aucun changement de comportement depuis ces captures (seul un repli de ligne Prettier), elles restent représentatives.
- [x] Vérification finale de clôture de lot (rejouée intégralement lors de la revue post-clôture) : `dotnet build` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 172/172 ; aucune nouvelle migration ce lot (filtres de requête et action `PUT` uniquement, aucune nouvelle colonne) ; `npx tsc -b` → 0 erreur ; `npm run test` → 85/85 ; `npm run lint` → 0 erreur (2 avertissements préexistants `react-refresh/only-export-components` sur `AvailabilityPage.tsx`/`TimeEntryForm.tsx`, non bloquants) ; `npm run build` → succès ; `npm run format:check` → conforme sur les 10 fichiers du Lot 9 (voir ci-dessus ; 105 fichiers antérieurs toujours non conformes, hors périmètre).

#### Validation visuelle — Lot 9

Captures Playwright/Chromium prises en conditions réelles (API Development/SQLite + frontend `npm run dev`), conservées dans `docs/screenshots/lot-9/` :

| Capture | Contenu |
|---|---|
| `00-accueil-sans-identite.png` | Layout avant sélection d'identité — sidebar avec les entrées `Temps`/`Mes absences`/`Disponibilités` déjà visibles (aucune garde de sidebar sur ces 3 entrées, cohérent avec l'absence de garde serveur de niveau contrôleur). |
| `01-accueil-identite-bernard.png` | Après sélection d'Alexandre BERNARD (`s636140`). |
| `02-temps-liste.png` | `/temps` : liste des saisies avec coût réel affiché (`FINANCIAL_DATA_VIEW` présente), filtres ressource/type/période. |
| `03-temps-modal-creation.png` | Formulaire de création d'une saisie de temps (`Modal`, type d'activité/date/durée/projet/commande/référence/commentaire). |
| `04-absences-liste.png` | `/mes-absences` : KPI mensuel/annuel/taux de disponibilité, liste avec statut et action de workflow disponible. |
| `05-absences-modal-creation.png` | Formulaire de création d'une absence. |
| `06-disponibilites-vue-mensuelle.png` | `/disponibilites`, vue Mensuelle : tableau « Capacité par ressource » (13 ressources) + calendrier coloré, week-ends non coloriés. |
| `07-disponibilites-vue-hebdomadaire.png` | Même écran, vue Hebdomadaire : bornes lundi-dimanche vérifiées (`Du 2026-07-20 au 2026-07-26`). |
| `08-temps-filtre-semaine.png` | `/temps` (revue de clôture) : filtres Projet/Commande/Semaine ajoutés et KPI « Total (filtre courant) » — sélection du 15/07/2026 dans « Semaine (jour contenu) » renseigne Du = 13/07/2026, Au = 19/07/2026 (bornes lundi-dimanche), liste et total recalculés en conséquence. |

### Code — Lot 10 (2026-07-21)

Troisième lot de la phase « Frontend complet (V1) » : Projets (liste + fiche détail à 7 onglets), Planning projet (vue transverse), sur la base de l'API des Lots 1-9. Plusieurs évolutions backend ponctuelles, chacune validée individuellement avec l'utilisateur avant implémentation à l'ouverture du lot (voir « Exception de gouvernance — Lot 10 », `docs/BACKLOG_METIER.md`) — portée plus large que les Lots 8/9 (un nouvel endpoint d'agrégation), mais toujours sans nouvelle entité ni nouvelle règle de calcul (les formules réutilisent `ProjectPlanningCalculator`, Lot 4, sans modification).

- [x] **`GET /api/v1/project-statuses` ajouté** (`docs/BACKLOG_METIER.md`, Décision 10) : `ProjectStatus` (référentiel administrable depuis le Lot 4, §16.2/§30) n'avait jamais eu d'endpoint de lecture — même écart que `CompanyType`/`Role` (Lot 8), corrigé ici plutôt que contourné par un second `knownReferentials.ts`, décision explicitement validée par l'utilisateur (option A). `ProjectStatusService`/`ProjectStatusesController`, lecture seule, aucune permission requise (référentiel non financier).
- [x] **`GET /api/v1/projects` étendu** (§16.1) : cinq nouveaux paramètres optionnels `teamId`, `niveauRisque`, `from`/`to` (période), `alertePlanning`, `alerteBudget` sur `ProjectService.GetListAsync`. `alertePlanning` traduit directement en prédicat SQL (`DateFinAjustee > DateFinPrevueInitiale`, même formule que `ProjectPlanningCalculator.CalculatePlanningRisk`). `alerteBudget` dépend de l'atterrissage financier (agrégation planning + snapshots) non traduisible en SQL : matérialisation du candidat filtré puis pagination en mémoire sur le total réellement filtré — jeu de projets de taille modeste (données de démonstration). Sans `FINANCIAL_DATA_VIEW`, `alerteBudget` renvoie une page vide plutôt que d'ignorer silencieusement le filtre demandé (même principe que l'omission de `FinancialSummary`, CLAUDE.md §13).
- [x] **`GET /api/v1/project-planning` créé** (§18.2, vue transverse "Planning projet") : agrégation entièrement serveur (`ProjectPlanningService.GetOverviewAsync`), une ligne par (Projet, Ressource, Semaine), filtres `projectId`/`resourceId`/`serviceId`/`departmentId`/`teamId`/`from`/`to`/`surcharge`, pagination/tri serveur. Décision actée avec l'utilisateur à l'ouverture du lot : remplace une agrégation par N appels frontend (`GET /projects/{id}/planning` répété par projet), écartée pour rester compatible avec une pagination/un tri corrects. Réutilise `AvailabilityService.GetAvailabilityAsync` (Lot 3, un seul appel par couple (ressource, semaine) distinct, jamais par ligne) pour la capacité réelle — aucune formule de capacité dupliquée. « Sous-charge » (§29.6) volontairement non filtrable : exige un seuil configurable absent de `Settings` (même écart que `AvailabilityResultDto`, Lot 3/9) — inventer un seuil non validé est exclu (CLAUDE.md §7).
- [x] **Lecture des lignes hebdomadaires ajoutée** (`ProjectPlanningService.GetWeeklyPlansAsync`, `GET plan-versions/{versionId}/weekly-plans`) : `SetWeeklyPlansAsync` n'était qu'en écriture depuis le Lot 4, sans moyen de relire la grille pour l'affichage ou la pré-remplir avant modification — écart constaté à l'ouverture du Lot 10.
- [x] **Refactoring sans changement de comportement** : `ProjectService.GetCoutReelConsommeAsync` déplacé vers `ProjectPlanningService` (méthode privée) pour que `ProjectService` puisse à son tour dépendre de `ProjectPlanningService` (filtre `alerteBudget` ci-dessus) sans dépendance circulaire entre les deux services — `ProjectPlanningService` ne dépendait plus que de `ProjectService` dans l'autre sens, désormais découplé.
- [x] **Jeu de données de démonstration enrichi** (§35, `Lot10Seed.cs`) : 6 projets supplémentaires (8 au total, variété volontaire sur tous les axes de filtrage du §16.1 — statut, application, pilote, service, équipe, niveau de risque, période, alerte planning), 12 jalons supplémentaires (15 au total, incluant une dépendance de jalon), 13 participants, versions de planning + lignes hebdomadaires pour 3 projets supplémentaires (démontrant la vue transverse sur plusieurs projets/ressources/semaines), 3 budgets. Uniquement des données, aucune nouvelle règle métier (décision actée à l'ouverture du lot). Migration `AddLot10ProjectsSeedAndPlanning` générée pour les **trois** providers ; appliquée et validée réellement sur SQLite (redémarrage de l'API à froid avec base vide, migrations appliquées automatiquement, `GET /api/v1/projects` → 8, `GET /api/v1/milestones` → 15) ; PostgreSQL et SQL Server générés et compilables (design-time), écart reconduit depuis le Lot 0.
- [x] **Écran Liste des projets** (`/projets`, `ProjectsListPage.tsx`) : tous les filtres du §16.1 (statut, application, pilote, département, service, équipe, niveau de risque, période, alerte planning, alerte budget), colonnes non financières + `FinancialValue` (budget initial, omis sans `FINANCIAL_DATA_VIEW`), navigation liste → fiche par clic de ligne, création via `ProjectCreateForm`.
- [x] **Écran Fiche projet** (`/projets/:id`, `ProjectDetailPage.tsx`) : 7 onglets (Synthèse, Participants, Planning, Budget, Temps, Jalons, Références liées), bandeau KPI (budget, temps consommé, avancement, participants, jalons, alertes). **« Avancement » n'est jamais calculé côté frontend** : aucune formule n'est définie par le cahier des charges (§29.5/§29.6 ne couvrent que charge/planning/budget) — affiché « — » (décision actée à l'ouverture du lot, `docs/BACKLOG_METIER.md`). Onglet Participants : coût/temps consommé par participant agrégé côté frontend à partir de `GET /time-entries?projectId=&resourceId=` déjà filtré par permission — même principe que la totalisation automatique du Lot 9, aucune nouvelle règle serveur (§17.2, coût réel/contractuel/différentiel par participant restent absents, non couverts par le backend). Onglet Planning : `WeeklyPlanningGrid` (nouveau composant transverse), édition de la charge Ajustée si une version Ajustée Active existe, création d'une nouvelle version Ajustée (motif obligatoire, §18.3). Onglet Budget : `GET /budgets?projectId=` sous `PermissionGuard`. Onglet Temps : agrégations par personne/semaine calculées côté frontend (mêmes données déjà filtrées par permission), temps détaillés. Onglet Jalons : `Timeline` (nouveau composant transverse, axe chronologique, pas un Gantt), création/modification via `MilestoneForm` (sélection de dépendance, aucune détection de cycle, décision actée Lot 4 reconduite). Onglet Références liées : `GET /reporting/projects/{id}/linked-references` (Lot 5, réutilisé sans modification).
- [x] **Écran Planning projet** (`/planning-projet`, `ProjectPlanningPage.tsx`) : vue transverse consommant `GET /project-planning`, filtres serveur (projet/ressource/service/département/équipe/période/surcharge), pagination/tri serveur — aucune agrégation côté frontend.
- [x] **Composants transverses créés** (`src/components/ui/`, cahier des charges §32.2, premiers consommateurs) : `Timeline` (axe chronologique générique, `label`/`statusLabel`/`statusTone` fournis par le consommateur — même principe d'agnosticisme du domaine que `StatusBadge`, Lot 7) et `WeeklyPlanningGrid` (grille hebdomadaire de présentation, aucun calcul, édition optionnelle via `onAjusteChange`).
- [x] **Écart backend `OperationalRole` sans endpoint `GET`, contourné** (même pattern que `CompanyType`/`Role`, Lot 8) : `OPERATIONAL_ROLE_OPTIONS`/`getOperationalRoleLabel` ajoutés à `src/lib/knownReferentials.ts` (valeurs seedées au Lot 1) plutôt qu'un nouvel endpoint non validé avec l'utilisateur pour ce lot — à remplacer dès qu'un référentiel réel existera.
- [x] Client API frontend étendu : `src/api/types.ts` (`ProjectDto` complété — `descriptionCourte`, `financialSummary` — `ProjectFinancialSummaryDto`, `ProjectStatusDto`, `ProjectParticipantDto`, `ProjectPlanVersionDto`, `ProjectWeeklyPlanDto`, `ProjectPlanningSynthesisDto`, `ProjectPlanningRowDto`, `ProjectLinkedReferenceDto`, `MilestoneDto`, `BudgetDto`), 6 nouveaux modules `src/api/endpoints/*.ts` (`projectStatuses`, `projectParticipants`, `projectPlanning`, `milestones`, `budgets`, extension de `projects`/`reporting`), même convention que les Lots 7-9.
- [x] Tests backend : 15 nouveaux cas dans `ProjectsTests.cs`/`ProjectPlanningLot10Tests.cs` (intégration, base SQLite réelle) — référentiel `project-statuses` ordonné, filtres étendus de `GET /projects` (équipe, niveau de risque, période, alerte planning, alerte budget avec/sans permission), lecture des lignes hebdomadaires, vue transverse `project-planning` (filtrage par projet/ressource, réalisé exclusivement issu des saisies de temps, partitionnement surcharge sans perte). Deux tests préexistants mis à jour pour refléter le seed enrichi (`ProjetsActifs` 2 → 5, fermeture de tous les budgets actifs en mode Complet plutôt qu'un seul). Suite complète : **185/185 réussis** (13 nouveaux + 2 mis à jour + 172 des Lots 0-9).
- [x] Tests frontend (Vitest + RTL), **20 nouveaux cas, 0 régression** : composants transverses `Timeline` (3 cas) et `WeeklyPlanningGrid` (3 cas, y compris édition). **Tests d'interaction au niveau écran** (`ProjectsListPage.test.tsx` 4 cas, `ProjectDetailPage.test.tsx` 7 cas, `ProjectPlanningPage.test.tsx` 3 cas) couvrant affichage, filtres (statut/niveau de risque/alertes/surcharge), création (projet, jalon), modification (projet), navigation entre onglets, permissions financières (Budget masqué/affiché selon `FINANCIAL_DATA_VIEW`), KPI (avancement toujours « — », temps consommé, alertes). Suite complète : **105/105 réussis** (20 nouveaux + 85 des Lots 0-9).
- [x] Vérification manuelle en conditions réelles (API Development/SQLite, base réinitialisée puis migrée à froid) : `GET /api/v1/project-statuses`, `GET /api/v1/projects` (8 résultats), `GET /api/v1/milestones` (15 résultats), `GET /api/v1/project-planning` vérifiés directement (réponses JSON conformes aux DTO), frontend (`npm run dev`) servi et proxy `/api` vers l'API vérifié fonctionnel.
- [x] **Validation visuelle en navigateur réalisée** (session ultérieure, Claude in Chrome disponible) : voir « Validation visuelle — Lot 10 » ci-dessous — écart du constat initial (ci-dessus) désormais couvert.
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 185/185 ; migration `AddLot10ProjectsSeedAndPlanning` appliquée et validée réellement sur SQLite (base vide, redémarrage à froid) ; `npx tsc -b` → 0 erreur ; `npm run test` → 105/105 ; `npm run lint` → 0 erreur (2 avertissements préexistants non bloquants, inchangés depuis le Lot 9) ; `npm run build` → succès ; `npm run format:check` → conforme sur l'intégralité des fichiers du Lot 10 (aucun des 112 fichiers non conformes détectés sur l'ensemble du dépôt — écart préexistant `core.autocrlf`, Lot 8 — n'appartient à ce lot).

#### Validation visuelle — Lot 10

Réalisée a posteriori (Claude in Chrome, session ultérieure à la rédaction initiale ci-dessus) : API (Development/SQLite, base réinitialisée puis migrée à froid, `dotnet ef database update`) et frontend (`npm run dev`) démarrés en conditions réelles, identité de démonstration « Alexandre BERNARD » sélectionnée (permissions financières actives). Les 3 écrans du lot ont été parcourus dans Chrome : Liste des projets (`/projets`, filtres §16.1, 8 projets), Fiche projet (`/projets/:id`, 7 onglets vérifiés individuellement — Synthèse, Participants, Planning, Budget, Temps, Jalons, Références liées), Planning projet (`/planning-projet`, vue transverse). **Aucune erreur console** détectée sur l'ensemble du parcours (`read_console_messages`, `onlyErrors`) ; seules 3 alertes `MUI: out-of-range value` de niveau *warning* (jamais *error*) ont été observées au rechargement complet de page — comportement du sélecteur d'identité de démonstration (`DemoIdentityProvider`, Lot 7), transitoire le temps que la liste des utilisateurs se charge, non introduit par ce lot et hors périmètre d'une correction ici. Captures conservées dans `docs/screenshots/lot-10/` : `01-projets-liste.jpg`, `02-projet-fiche-synthese.jpg`, `03-projet-fiche-planning.jpg`, `04-projet-fiche-jalons.jpg`, `05-projet-fiche-budget.jpg`, `06-planning-projet-transverse.jpg` (format `.jpg`, capture native de l'outil de navigation — contenu identique en substance aux captures `.png` des Lots 7/9).

### Code — Lot 11 (2026-07-21)

Quatrième et dernier lot de la phase « Frontend complet (V1) » construit sur des évolutions backend ponctuelles : Commandes (liste + fiche détail à 5 onglets, machine d'état), Budgets (indicateurs + lignes de budget), Jalons (vue transverse tableau/timeline/calendrier). Trois évolutions backend validées individuellement avant implémentation (voir « Exception de gouvernance — Lot 11 », `docs/BACKLOG_METIER.md`) — même nature que les Lots 9/10, aucune nouvelle entité ni migration.

- [x] **`GET /api/v1/order-statuses` ajouté** (`docs/BACKLOG_METIER.md`, Décision 2) : `OrderStatus` (référentiel administrable depuis le Lot 1, §13.2/§30) n'avait jamais eu d'endpoint de lecture — même écart que `CompanyType`/`Role` (Lot 8), corrigé ici comme `ProjectStatus` (Lot 10) plutôt que contourné par `knownReferentials.ts`. `OrderStatusService`/`OrderStatusesController`, lecture seule, aucune permission requise (référentiel non financier).
- [x] **`GET /api/v1/reporting/financial` étendu d'une `ConsommationMensuelle`** (`docs/BACKLOG_METIER.md`, Décision 3) : `FinancialReportMonthlyConsumptionDto` (année, mois, coût réel, coût contractuel, différentiel), calculé dans `ReportingService.GetFinancialReportAsync` par un `GroupBy` sur le mois des `TimeEntryFinancialSnapshot` déjà chargés pour cette méthode — même style que `DifferentielParProjet`/`ParCommande`/`ParSociete`/`ParRessource` (Lot 5), aucune nouvelle requête, aucune duplication de logique financière. Un mois n'apparaît que s'il porte au moins une saisie valorisée sur la période demandée.
- [x] **`GET /api/v1/milestones` étendu du paramètre `applicationId`** (`docs/BACKLOG_METIER.md`, Décision 4) : `MilestoneService.GetListAsync` accepte désormais un filtre optionnel par application (§24.3), même précédent que les extensions de filtres des Lots 9/10 sur une action déjà existante.
- [x] **Modèle Budget détaillé par ligne explicitement non construit** (`docs/BACKLOG_METIER.md` §2, Décision 1) : décision actée à l'ouverture du lot de rester sur le modèle `Budget`/`BudgetVersion` existant (§14.1/§14.2), conforme à la formulation de la roadmap — reporté à un futur lot dédié.
- [x] **Composants transverses factorisés** (demande explicite de l'utilisateur avant développement) : `DetailPageHeader` (titre/sous-titre/actions), `KpiBand` (bandeau de KPI + emplacement additionnel), `DetailTabs` (barre d'onglets), extraits de `ProjectDetailPage.tsx` (Lot 10, refactoring sans changement de comportement, vérifié par la suite de tests existante) et réutilisés dès leur création par `OrderDetailPage.tsx` — aucune duplication entre les deux fiches détail. `BudgetGauge` (nouveau, jauge de consommation budgétaire, enveloppe de `ProgressBar` explicitement anticipée pour cet usage depuis le Lot 7).
- [x] **Écran Commandes** (`/commandes`, `OrdersListPage.tsx`) : filtres société/statut/projet, colonnes non financières + colonnes financières via `FinancialValue` (`OrderDto.FinancialSummary`, absent sans `FINANCIAL_DATA_VIEW`), navigation liste → fiche par clic de ligne, création via `OrderCreateForm` (multi-sélection des ressources autorisées via `Autocomplete multiple`, même précédent ponctuel que `TechnologyForm.tsx`, Lot 8, §5 « pas d'abstraction anticipée »).
- [x] **Écran Fiche commande** (`/commandes/:id`, `OrderDetailPage.tsx`) : 5 onglets (Synthèse, Rallonges, Réceptions, Temps, Budgets liés), bandeau KPI (budget initial/ajusté, société, projet lié, statut), boutons de transition d'état contextuels au statut courant (Activer/Suspendre/Marquer consommée/Clôturer/Réouvrir avec motif obligatoire) — machine d'état déjà existante (Lot 5/6), aucune nouvelle règle. Onglet Rallonges : liste + création (`OrderExtensionCreateForm`, motif obligatoire §13.3). Onglet Réceptions : liste + résumé (reste réceptionnable) + création (`OrderReceiptCreateForm`) — le dépassement du reste réceptionnable n'est jamais un champ de formulaire : le serveur l'autorise silencieusement si l'appelant porte `ORDER_RECEIPT_OVERRIDE`, sinon 409 (un indice textuel s'affiche sans cette permission). Onglet Temps : saisies liées (`GET /time-entries?orderId=`, déjà filtrable depuis le Lot 9). Onglet Budgets liés : `GET /budgets?orderId=`, sous `PermissionGuard`.
- [x] **Écran Budgets** (`/budgets`, `BudgetsListPage.tsx`) : indicateurs (§14.3) entièrement dérivés de `GET /reporting/dashboard` (budget initial/ajusté total, commandes à risque, projets sous-financés — Lot 5, jamais consommé côté frontend avant ce lot) et `GET /reporting/financial` (différentiel global, budget restant, atterrissage estimé, différentiels par projet/commande/société/ressource, consommation mensuelle) — aucune agrégation dupliquée côté frontend. Graphique de consommation mensuelle (Recharts, barres groupées Coût réel/Coût contractuel) construit selon le skill `dataviz` : palette catégorielle validée par le script fourni (`validate_palette.js`, bleu `#2a78d6`/orange `#eb6834`, tous les contrôles PASS en mode clair et sombre). Lignes de budget (CRUD complet, `BudgetGauge` par ligne, ajustement historisé via `BudgetAdjustForm`, historique des versions, clôture/réactivation) — déjà exposées par `budgets.ts` (Lot 10) pour l'onglet Budget de la fiche projet, réutilisées ici sans modification. Écran entier gardé par `FINANCIAL_DATA_VIEW`.
- [x] **Écran Jalons** (`/jalons`, `MilestonesListPage.tsx`) : vue transverse tous projets, 3 vues (§24.3) — Tableau (`DataTable`-like, colonne « Alerte planning projet » dérivée du projet lié), Timeline (réutilise le composant `Timeline` du Lot 10 tel quel, désormais multi-projets), Calendrier (nouvelle grille mensuelle propre à cet écran — **`AvailabilityPage.tsx` du Lot 9 n'a pas été modifié** pour en extraire un composant partagé, conformément à la consigne de ne pas rouvrir les lots précédents ; l'extraction d'un `CalendarView` commun reste une opportunité de nettoyage future). Filtres projet/application (nouveau)/responsable/statut/retard. Compteur « à venir sous 30 jours » et badge « Risque planning projet » calculés côté frontend sur des données déjà exposées (même principe que la totalisation automatique du Lot 9 et le filtre `alertePlanning` du Lot 10) — aucune nouvelle agrégation serveur. Création de jalon : sélection du projet d'abord (le formulaire `MilestoneCreateForm`, Lot 10, exige un `projectId` — réutilisé tel quel, sans modification), puis formulaire existant.
- [x] Client API frontend étendu : `src/api/types.ts` (`OrderDto.FinancialSummary`, `OrderCreateRequest`/`UpdateRequest`/`ReopenRequest`, `OrderExtensionDto`, `OrderReceiptDto`/`SummaryDto`, `OrderStatusDto`, `DashboardDto`/`DashboardOperationalKpisDto`/`DashboardFinancialKpisDto`, `FinancialReportDto` + sous-types), 4 nouveaux modules `src/api/endpoints/*.ts` (`orderStatuses`, `orderExtensions`, `orderReceipts`, extension de `orders`/`milestones`/`reporting`), même convention que les Lots 7-10.
- [x] Tests backend : 3 nouveaux cas (`OrderStatusesTests`-like dans `BudgetsAndReportingTests.cs`/`ProjectsTests.cs`) — référentiel `order-statuses` ordonné, ventilation mensuelle du reporting financier cohérente avec un snapshot fraîchement créé, filtre `applicationId` sur `GET /milestones`. Suite complète : **188/188 réussis** (3 nouveaux + 185 des Lots 0-10).
- [x] Tests frontend (Vitest + RTL), **31 nouveaux cas, 0 régression** : composants transverses `DetailPageHeader` (2 cas), `KpiBand` (2 cas), `DetailTabs` (2 cas), `BudgetGauge` (2 cas). Tests d'interaction au niveau écran : `OrdersListPage.test.tsx` (3 cas), `OrderDetailPage.test.tsx` (5 cas — synthèse financière, permission, transition d'état, création de rallonge, résumé des réceptions), `BudgetsListPage.test.tsx` (4 cas — indicateurs + graphique, permission, clôture, création), `MilestonesListPage.test.tsx` (5 cas — tableau + alerte planning + compteur 30 jours, filtre application, timeline, calendrier, création). `ProjectDetailPage.test.tsx` (Lot 10, inchangé) reste vert après le refactoring des composants transverses. Suite complète : **130/130 réussis** (31 nouveaux + 8 composants transverses + 105 des Lots 0-9, moins la revue Lot 10 déjà comptée — total net 130 sur l'ensemble du dépôt).
- [x] Validation visuelle en navigateur (Claude in Chrome) : API (Development/SQLite) et frontend (`npm run dev`) démarrés en conditions réelles, identité « Alexandre BERNARD » (permissions financières). Les 3 écrans parcourus intégralement : Commandes (liste, fiche détail avec ses 5 onglets, formulaire de réception ouvert), Budgets (indicateurs, graphique mensuel, historique d'ajustement, clôture testée), Jalons (3 vues, filtre application, ouverture du formulaire d'édition depuis le calendrier). **Aucune erreur console** sur l'ensemble du parcours. Détail et captures : voir « Validation visuelle — Lot 11 » ci-dessous.
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 188/188 ; `npx tsc -b` → 0 erreur ; `npm run test` → 130/130 ; `npm run lint` → 0 erreur (2 avertissements préexistants non bloquants, inchangés depuis le Lot 9) ; `npm run build` → succès (avertissement de taille de bundle non bloquant, pré-existant, Recharts ajouté — le découpage en chunks reste explicitement hors périmètre, Lot 13) ; `npm run format:check` → conforme sur l'intégralité des fichiers du Lot 11 (vérifié fichier par fichier, aucun des 129 fichiers non conformes détectés sur l'ensemble du dépôt n'appartient à ce lot — écart préexistant `core.autocrlf`, Lot 8).

#### Validation visuelle — Lot 11

API (Development/SQLite, base existante réutilisée — aucune migration ce lot) et frontend (`npm run dev`) démarrés en conditions réelles, identité de démonstration « Alexandre BERNARD » sélectionnée (permissions financières actives). **Commandes** (`/commandes`) : liste (1 commande seedée, filtres société/statut/projet), fiche détail (`/commandes/:id`) avec ses 5 onglets vérifiés individuellement — Synthèse (financier), Rallonges (rallonge seedée affichée), Réceptions (résumé + réception seedée, formulaire de création ouvert et vérifié), Budgets liés. **Budgets** (`/budgets`) : indicateurs (§14.3) et graphique de consommation mensuelle affichés avec des données réelles, lignes de budget avec `BudgetGauge`, historique d'un ajustement ouvert et vérifié. **Jalons** (`/jalons`) : vue Tableau (15 jalons, colonne alerte planning projet), vue Timeline, vue Calendrier (grille mensuelle avec jalons positionnés sur leur jour, formulaire d'édition ouvert depuis un jalon du calendrier), filtre application vérifié (VTOM → 5 jalons, total recalculé). **Aucune erreur console** détectée sur l'ensemble du parcours (`read_console_messages`, `onlyErrors`, 144 messages inspectés) ; seules les mêmes 3 alertes `MUI: out-of-range value` de niveau *warning* déjà documentées au Lot 10 (sélecteur d'identité, Lot 7, non introduites par ce lot) ont été observées. Captures conservées dans `docs/screenshots/lot-11/` : `01-commandes-liste.jpg`, `02-commande-fiche-synthese.jpg`, `03-commande-fiche-rallonges.jpg`, `04-commande-fiche-receptions.jpg`, `05-budgets-indicateurs.jpg`, `06-budgets-lignes.jpg`, `07-jalons-tableau.jpg`, `08-jalons-timeline.jpg`, `09-jalons-calendrier.jpg`.

### Code — Lot 12 (2026-07-21)

Dernier lot de la phase « Frontend complet (V1) » : Charges (indicateurs, tops, répartition RUN/hors RUN, capacité vs réalisé, prévu vs réalisé, heatmap, évolution mensuelle), Tableau de bord (`/`, KPI opérationnels et financiers, mêmes graphiques recomposés), Reporting (rapports opérationnel/financier §26, export CSV/Excel/PDF), Imports (assistant complet §27, historique des lots, comparaison de diffs). Trois évolutions backend ciblées, validées individuellement avant implémentation après une réserve explicite de l'utilisateur sur la nécessité réelle de l'agrégation « Prévu vs réalisé » côté serveur (voir « Exception de gouvernance — Lot 12 » et Décision 3, `docs/BACKLOG_METIER.md`) — aucune nouvelle entité, aucune nouvelle migration.

- [x] **`GET /api/v1/reporting/charges` étendu d'une évolution mensuelle et d'une heatmap** (`docs/BACKLOG_METIER.md`, Décision 1) : `ChargesMonthlyEvolutionDto` (année, mois, charge totale/RUN/hors RUN) via un `GroupBy` sur `(Année, Mois)` des saisies déjà chargées par `GetChargesReportAsync` ; `ChargesHeatmapEntryDto` (ressource × début de semaine, charge en heures) via un `GroupBy` sur `(ResourceId, DébutDeSemaine)` — `DébutDeSemaine` calculé par un helper `GetWeekStart` (lundi ISO), même style que les autres agrégations du service (`TopApplications`/`TopUtilisateurs`, Lot 5), aucune duplication de logique.
- [x] **`GET /api/v1/reporting/charges` étendu d'une comparaison Prévu vs réalisé** (`docs/BACKLOG_METIER.md`, Décision 3, justifiée explicitement après la réserve de l'utilisateur) : `ChargesPlanComparisonDto.ChargePrevue` calculé par `BuildPlanComparisonAsync` à partir de `ProjectWeeklyPlan` (version Active uniquement — même convention que `ProjectPlanningService.GetOverviewAsync`, Lot 10), `null` si le filtre porte sur `orderId`/`activityTypeId` (aucune notion de commande/type d'activité sur un planning). Construit côté backend pour 4 raisons vérifiées avant développement (aucune donnée déjà exposée à réutiliser, parité de filtres manquante sur `GET /project-planning`, agrégation sur des données paginées plutôt qu'une liste déjà chargée, localité de la règle « seule la version Active compte ») — détail complet dans `docs/BACKLOG_METIER.md` §16.
- [x] **`GET /api/v1/reporting/operational/export` ajouté** (`docs/BACKLOG_METIER.md`, Décision 2) : mirror exact de `ExportCharges`/`ExportFinancial` (Lot 6), `ReportingService.GetOperationalTableAsync` construit un `ReportingTableDto` (colonnes Dimension/Nom/Valeur/Détail) à partir de `OperationalReportDto` déjà exposé par `GET /reporting/operational` (Lot 5) — aucune permission requise (aucune donnée financière), même moteur `ExportService` générique.
- [x] **Mapping CSV → DTO des imports confirmé en lecture seule** (`docs/BACKLOG_METIER.md`, Décision 4) : aucun assistant de correspondance interactif construit ce lot — conforme à `CLAUDE.md` §21 (différé, aucun écran n'existait avant ce lot). L'écran Imports affiche la comparaison en-têtes détectées/attendues à titre d'information uniquement.
- [x] **Composants transverses anticipés par `docs/ARCHITECTURE.md` §3 construits** : `WorkloadHeatmap` (grille ressource × semaine, rampe séquentielle bleue validée par le skill `dataviz`), `ChartCard` (enveloppe `Card`/`CardHeader`/`CardContent`), `ImportWizard` (machine à états à 7 étapes Type → Mode → Fichier → Aperçu → Simulation → Confirmation → Compte rendu, bouton « Suivant » désactivé tant que `errorCount > 0`), `DiffViewer` (table de diffs avec badge de statut par type).
- [x] **Écran Charges** (`/charges`, `ChargesPage.tsx`) : 10 filtres (période personnalisée, application, projet, commande, département, service, équipe, utilisateur, type d'activité, rôle opérationnel), indicateurs, 4 tops, répartition RUN/hors RUN (barre empilée à 100 %, palette catégorielle validée `#2a78d6`/`#eb6834`), capacité vs réalisé (dérivé de `GET /reporting/dashboard`, aucune duplication), prévu vs réalisé (message « Non calculable sur ce périmètre » quand `chargePrevue` est `null`), courbe mensuelle, heatmap, tableaux surcharge/sous-charge.
- [x] **Écran Tableau de bord** (`/`, `DashboardPage.tsx`) : KPI opérationnels toujours visibles, KPI financiers sous `PermissionGuard`, 3 graphiques de comparaison entièrement recomposés à partir des endpoints déjà exposés (Charges, Dashboard, Financial, Milestones) — aucune agrégation dupliquée, aucun widget configurable. Jalons à venir (fenêtre de 30 jours calculée côté frontend sur `GET /milestones`, même principe que le calcul déjà accepté au Lot 9/11).
- [x] **Écran Reporting** (`/reporting`, `ReportingPage.tsx`) : rapport opérationnel (toujours visible) et rapport financier (`PermissionGuard`) déjà exposés par `GET /reporting/operational`/`GET /reporting/financial` (Lot 5), sélecteur de format d'export (CSV/Excel/PDF) et deux boutons d'export déclenchant un téléchargement de fichier réel (`Content-Disposition` parsé côté frontend, `lib/download.ts`, nouveau — premier écran du projet à déclencher un téléchargement de fichier binaire).
- [x] **Écran Imports** (`/imports`, `ImportsPage.tsx`) : assistant complet (`ImportWizard`) couvrant les 16 types importables du §27.1, historique des lots avec filtre par type, ouverture d'un lot passé dans une modale `DiffViewer`. Les 4 appels FormData (`previewImport`/`simulateImport`/`executeImport`/`executeSharePointImport`) forcent `Content-Type: undefined` pour laisser le navigateur poser la frontière multipart lui-même (le client HTTP central pose `application/json` par défaut, ce qui aurait cassé l'upload sans ce correctif).
- [x] Client API frontend étendu : `src/api/types.ts` (`ExportFormat`, `ChargesMonthlyEvolutionDto`/`HeatmapEntryDto`/`PlanComparisonDto`, `OperationalReportDto` + sous-types, `ImportEntityType`/`ImportMode`/`ImportBatchStatus`/`ImportDiffType` + DTO associés), nouveau module `src/api/endpoints/imports.ts`, extension de `reporting.ts` (export de fichiers), même convention que les Lots 7-11.
- [x] Tests backend : 6 nouveaux cas dans `BudgetsAndReportingTests.cs` — évolution mensuelle et heatmap sur la période seedée, comparaison Prévu vs réalisé avec version Ajustée active, comparaison `null` quand filtrée par commande/type d'activité, export opérationnel dans les 3 formats (`Theory`). Suite complète : **194/194 réussis** (6 nouveaux + 188 des Lots 0-11).
- [x] Tests frontend (Vitest + RTL), nouveaux cas sans régression : `ChartCard` (1 cas), `WorkloadHeatmap` (2 cas), `DiffViewer` (2 cas), `ChargesPage.test.tsx` (3 cas — indicateurs/tops, heatmap/courbe mensuelle, message non calculable), `DashboardPage.test.tsx` (2 cas — KPI opérationnels seuls, KPI financiers avec permission), `ReportingPage.test.tsx` (3 cas — rapport opérationnel + repli financier, rapport financier avec permission, export), `ImportsPage.test.tsx` (3 cas — historique des lots, flux complet de l'assistant type→mode→fichier→aperçu→simulation→confirmation→compte rendu, ouverture du `DiffViewer` sur un lot passé). Suite complète : **146/146 réussis**.
- [x] Validation visuelle en navigateur (Claude in Chrome) : API (Development/SQLite) et frontend (`npm run dev`) démarrés en conditions réelles, identité « Alexandre BERNARD » sélectionnée (permissions financières). Les 4 écrans parcourus intégralement, y compris un import réel de bout en bout (fichier CSV Application, une erreur de FK délibérément provoquée puis corrigée pour vérifier le blocage du bouton « Suivant », simulation propre, exécution confirmée, lot visible dans l'historique, diff ouvert dans la modale). **Aucune erreur console** sur l'ensemble du parcours. Détail et captures : voir « Validation visuelle — Lot 12 » ci-dessous.
- [x] Vérification finale de clôture de lot : `dotnet build --no-incremental` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 194/194 ; `npx tsc -b` → 0 erreur ; `npx vitest run` → 146/146 ; `npm run lint` → 0 erreur (2 avertissements préexistants non bloquants, inchangés depuis le Lot 9) ; `npm run format:check` → conforme sur l'intégralité des fichiers du Lot 12 (vérifié fichier par fichier via `npx prettier --check` sur la liste exacte des fichiers nouveaux/modifiés — écart préexistant `core.autocrlf`, Lot 8, non lié à ce lot).

#### Validation visuelle — Lot 12

API (Development/SQLite, base existante réutilisée — aucune migration ce lot) et frontend (`npm run dev`) démarrés en conditions réelles. **Découverte d'environnement locale reconduite** (déjà documentée au Lot 9, non retranchée depuis) : au moment de cette validation, `vite.config.ts` (proxy `/api` → port `5297`) et `launchSettings.json` (profils → port `5215`) étaient encore en désaccord ; l'API avait été démarrée avec `--urls http://localhost:5297 --no-launch-profile` pour faire correspondre le proxy. **Corrigé avant la clôture du lot** (voir « Nouveaux écarts constatés au Lot 12 » ci-dessous) : `vite.config.ts` cible désormais `5215`, `dotnet run`/`npm run dev` sans paramètre fonctionnent ensemble — ce contournement ne sera plus nécessaire pour les lots suivants. Identité de démonstration « Alexandre BERNARD » sélectionnée (permissions financières actives). **Tableau de bord** (`/`) : KPI opérationnels et financiers avec données réelles, 3 graphiques de comparaison rendus. **Charges** (`/charges`, période 01/01/2024–21/07/2026 pour couvrir les données seedées) : indicateurs et tops réels, répartition RUN/hors RUN, capacité vs réalisé, prévu vs réalisé, heatmap (4 ressources × 3 semaines), courbe mensuelle, tableaux surcharge/sous-charge. **Reporting** (`/reporting`) : rapport opérationnel et rapport financier avec données réelles, export opérationnel déclenché et vérifié (requête `200`). **Imports** (`/imports`) : assistant parcouru intégralement avec un fichier CSV réel (type Application) — une erreur métier réelle (« Le responsable indiqué n'existe pas ») a d'abord bloqué le bouton « Suivant » comme attendu, puis une correction du fichier a permis une simulation propre (Ajouts : 1, Erreurs : 0) et une exécution confirmée ; le nouveau lot apparaît dans l'historique et sa modale de diffs s'ouvre correctement. **Aucune erreur console** détectée sur l'ensemble du parcours (`read_console_messages`, `onlyErrors`) ; seules les mêmes alertes `MUI: out-of-range value` déjà documentées depuis le Lot 7 (sélecteur d'identité, transitoire) ont été observées. Captures conservées dans `docs/screenshots/lot-12/` : `01-dashboard-kpis-financiers.jpg`, `02-charges-heatmap-evolution.jpg`, `03-charges-indicateurs-tops.jpg`, `04-reporting-operationnel-financier.jpg`, `05-imports-apercu.jpg`, `06-imports-simulation.jpg`, `07-imports-compte-rendu.jpg`, `08-imports-diffviewer.jpg`.

### Code — Lot 13 (2026-07-22)

Premier lot de la phase renommée « Authentification, RBAC, Sécurisation API, CI/CD et Qualité » (voir « Révision de la roadmap », `docs/ROADMAP.md`) : quatre décisions validées avant tout développement, avec deux ajustements complémentaires validés dans un second temps (nommage `IAuthenticationProvider`/`DemoAuthenticationProvider`, modèle `UserSession` compatible dès ce lot avec la distinction session navigateur/persistante, structure CI extensible pour Sonar/Docker/déploiement — détail complet dans `docs/BACKLOG_METIER.md` §17).

- [x] **Authentification simulée sessionnée** (Décision 1) : `IAuthenticationProvider` (Application, `Common/Security/`) — cycle de vie de session, seule abstraction connue du reste de l'application — implémenté par `DemoAuthenticationProvider` (Api). `UserSession` (Domain, nouvelle entité) : jeton opaque, `IsPersistent` (session navigateur/persistante, "se souvenir de moi" non exposée à l'écran ce lot mais modèle prêt), `ExpiresAt`/`LastActivityAt` (expiration glissante). `AuthController` (`POST`/`DELETE /api/v1/auth/sessions`) pose/efface un cookie HttpOnly/`SameSite=Strict`/`Secure` hors Development, portant uniquement le jeton. `IdentityResolutionMiddleware` résout l'identité une seule fois par requête (cookie, repli sur `X-Demo-User` si `Authentication:AllowDirectDemoHeader`) et dépose le résultat dans `HttpContext.Items` — `DemoCurrentUserProvider` (`ICurrentUser`, forme inchangée) le lit de façon synchrone.
- [x] **Modèle RBAC** (Décision 2) : `RolePermission` (Domain, nouvelle jointure) accorde des permissions par défaut à un rôle ; `UserPermission.Effect` (nouvelle colonne, `Grant`/`Revoke`) complète ou retire une exception individuelle, toujours prioritaire sur le rôle. `PermissionResolutionService` (Application) calcule les permissions effectives (`rôle ∪ octrois − retraits`), réutilisé par `ICurrentUser` et par `UserService`. Matrice de rôle seedée (`Lot13Seed.cs`) dérivée des affectations déjà existantes (aucune régression : `ADMINISTRATEUR` reçoit les 7 permissions que Bernard détenait déjà individuellement ; les 3 autres rôles n'en reçoivent aucune, l'unique cas ambigu — `FINANCIAL_DATA_VIEW` de Legrand — reste une exception individuelle). `UserService.GrantPermissionAsync`/`RevokePermissionAsync` rendus RBAC-conscients (conflit sur le résultat effectif, matérialisation d'un retrait explicite quand le rôle grante encore la permission après suppression de l'octroi individuel) — même contrat HTTP qu'avant.
- [x] **Périmètre organisationnel explicitement reporté** (Décision 3) : aucune règle département/service/équipe/propriété de la donnée introduite — voir « Nouveaux écarts constatés au Lot 13 » ci-dessous.
- [x] **Pipeline GitHub Actions** (Décision 4) : `.github/workflows/ci.yml`, deux jobs (`backend`, `frontend`), déclenchés sur `pull_request`/`push` vers `main` + `workflow_call` (invocable par un futur workflow de déploiement). Backend : build + tests + couverture (`dotnet test --collect:"XPlat Code Coverage"`), seuils vérifiés par `scripts/ci/check-backend-coverage.ps1` (projets `SafranTimeTracker.Migrations.*` exclus). Frontend : `npm ci`, `tsc -b`, lint, tests avec couverture (`@vitest/coverage-v8`, seuils dans `vite.config.ts`), build. Rapports publiés comme artefacts. Aucun SonarQube/SonarCloud, aucun Docker, aucun secret applicatif — structure commentée pour les accueillir sans refonte.
- [x] **Sécurisation de l'API** (périmètre initial, hors des 4 décisions) : en-têtes de sécurité (HSTS hors Development, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, CSP `default-src 'none'`), CORS étendu à `AllowCredentials()`, limitation de débit native .NET sur `POST /auth/sessions`, limite de taille de requête (10 Mo) sur les 4 endpoints d'upload d'import.
- [x] **Compatibilité des 194 tests backend existants** : préservée sans aucune modification de ces fichiers — `Authentication:AllowDirectDemoHeader=true` en Development/Test laisse `X-Demo-User` résoudre une identité sans session, exactement le chemin déjà emprunté par `CreateClient(identifiant)`.
- [x] **Migrations** : `AddLot13AuthenticationRbac`, sur les 3 providers (SQLite appliquée et vérifiée réellement, PostgreSQL/SQL Server compilées) — colonne additive `user_permissions.effect` (défaut 0, aucune ligne existante modifiée en comportement) + nouvelles tables `role_permissions`/`user_sessions`.
- [x] Frontend : `src/api/endpoints/auth.ts` (nouveau, `createDemoSession`/`revokeDemoSession`), `DemoIdentityProvider.tsx` établit/révoque la session à chaque changement d'identité et **restaure la session au montage** (correctif appliqué après détection en validation visuelle : sans cela, un rechargement de page affichait l'identité mémorisée sans session valide, provoquant des 403 silencieux) ; `client.ts` ne pose plus l'en-tête `X-Demo-User` (le cookie porte l'identité, aucun `withCredentials` nécessaire — même origine via le proxy Vite/IIS) ; `useCurrentUser.ts` lit directement `UserDto.effectivePermissionCodes` (Lot 13) au lieu de joindre `permissionIds` à `GET /permissions`.
- [x] Tests backend : 8 nouveaux cas (`Lot13AuthenticationRbacTests.cs`) — création/révocation de session par cookie sans en-tête, identifiant inconnu (401), accès financier via session puis après révocation (403), permissions effectives d'un rôle Administrateur et d'un rôle Ingénieur sans exception, conflit d'octroi déjà effectif via le rôle, matérialisation d'un retrait explicite puis ré-octroi. Suite complète : **202/202 réussis** (8 nouveaux + 194 des Lots 0-12, aucune modification des fichiers existants).
- [x] Tests frontend : mise à jour de 14 fichiers de test existants (ajout du mock `api/endpoints/auth`, requis par la restauration de session au montage) + `effectivePermissionCodes` ajouté aux fixtures `UserDto` de 16 fichiers. Suite complète : **146/146 réussis**, aucune régression.
- [x] Mesure réelle de couverture avant fixation des seuils (jamais un chiffre inventé) : backend par projet (Api 64-67 %/30-40 % lignes/branches, Application 74 %/55 %, Domain 93 %/100 %, Infrastructure 97 %/64 %, `Migrations.*` exclus du calcul) ; frontend globale (statements 72,6 %, branches 71,6 %, functions 64,3 %, lines 72,2 %) — seuils fixés en dessous avec marge de sécurité.
- [x] Validation visuelle en navigateur (Claude in Chrome) : API (Development/SQLite, migration Lot 13 appliquée à la base de développement existante) et frontend (`npm run dev`) démarrés en conditions réelles. Session établie pour Alexandre BERNARD (Administrateur, RBAC via rôle) — KPI financiers et navigation Budgets/Imports/Administration visibles ; bascule vers Marco COSTA (Ingénieur, aucune permission de rôle ni individuelle) — KPI financiers et navigation gardée disparaissent immédiatement, confirmant le calcul RBAC serveur. Rechargement de page vérifié après le correctif de restauration de session (`POST /auth/sessions` rejoué automatiquement, `GET /reporting/financial` passe de 403 à 200). **Aucune erreur console** sur l'ensemble du parcours. Captures dans `docs/screenshots/lot-13/` : `01-session-cookie-administrateur-rbac.jpg`, `02-rbac-ingenieur-sans-acces-financier.jpg`.
- [x] Vérification finale de clôture de lot : `dotnet build` (solution complète) → 0 avertissement, 0 erreur ; `dotnet test` → 202/202 ; `npx tsc -b` → 0 erreur ; `npx vitest run` → 146/146 ; `npm run lint` → 0 erreur (2 avertissements préexistants inchangés) ; `npm run build` → succès (avertissement de taille de bundle préexistant, toujours différé — non demandé dans le périmètre validé de ce lot) ; seuils de couverture backend et frontend respectés.

## Écarts connus par rapport au cahier des charges / à la roadmap

**Écarts transversaux résolus au Lot 6 :** `AuditLog` était mentionné comme « toujours absent » dans plusieurs sections d'écarts ci-dessous (Lots 0 à 5) — il existe désormais et couvre l'intégralité de la liste minimale du §28.3 (voir « Code — Lot 6 » ci-dessus). Ces mentions historiques ne sont **pas** réécrites individuellement : chaque section « Écarts constatés au Lot X » est un instantané daté de l'état réel à la clôture de ce lot, pas un statut vivant. De même, « Rôles et permissions — toujours partiel » (Lot 0) est désormais résolu pour l'axe permission (administration utilisateur complète, §28.3) ; la restriction par périmètre organisationnel (département/service/équipe) reste un écart ouvert — non traitée par le nouveau Lot 7 (Frontend Foundation, qui ne consomme que les permissions déjà exposées par l'API), reportée aux lots suivants (voir ci-dessous).

**Écarts transversaux résolus au Lot 7 (révision de roadmap) :** « Design system — toujours non mis en place » (mentionné aux Lots 0 à 6) est désormais résolu (voir « Code — Lot 7 » ci-dessus). « Aucun écran frontend pour X » reste en revanche vrai pour toutes les données métier : le Lot 7 construit exclusivement les fondations (layout, navigation, composants transverses, permissions côté affichage) — **aucun écran métier** n'est livré, conformément au périmètre validé par l'utilisateur et à la nouvelle roadmap (`docs/ROADMAP.md`, Lots 8 à 12).

**Écarts transversaux résolus au Lot 8 :** « Aucun endpoint d'agrégation dédié à une fiche application » (Lot 1) est désormais résolu — la fiche Application réutilise `GET /api/v1/reporting/charges` (Lot 5), filtré par `applicationId`, sans nouvelle agrégation backend. « Administration regroupera 13 sous-onglets aux permissions hétérogènes » (annoncé au Lot 7) est confirmé : l'entrée de sidebar `Administration` reste sans garde de permission de niveau contrôleur (les gardes existent au niveau de chaque sous-onglet — `USER_ADMINISTRATION` pour Utilisateurs, `AUDIT_VIEW` pour Audit — pas au niveau du panneau lui-même), ce n'est pas un écart mais la conséquence attendue de l'hétérogénéité des permissions par onglet.

**Écarts transversaux résolus au Lot 9 :** aucun écart transversal antérieur n'est fermé par ce lot (les écarts Lots 0-8 concernaient l'authentification, le périmètre organisationnel, PostgreSQL/SQL Server ou des endpoints d'administration — tous hors du périmètre frontend-only de ce lot). « Aucun écran frontend pour le temps et la capacité » (Lot 3) et « pour les absences » sont en revanche résolus : voir « Code — Lot 9 » ci-dessus.

**Écarts transversaux résolus au Lot 10 :** « `CompanyType`/`Role` sans endpoint `GET` » (Lot 8) trouve un nouveau cas apparenté résolu différemment : `ProjectStatus` avait la même lacune, mais a été corrigée par un vrai endpoint (`GET /api/v1/project-statuses`) plutôt que contournée — décision explicitement validée par l'utilisateur (option A, `docs/BACKLOG_METIER.md` Décision 10). `CompanyType`/`Role` restent en revanche non résolus (hors périmètre de ce lot). « Aucun écran frontend pour les projets/le planning/les jalons » (Lots 4, écarts reconduits jusqu'au Lot 9) est désormais résolu : voir « Code — Lot 10 » ci-dessus.

**Écarts transversaux résolus au Lot 11 :** « `OrderStatus` sans endpoint `GET` » trouve le même traitement que `ProjectStatus` (Lot 10) : corrigé par un vrai endpoint (`GET /api/v1/order-statuses`) plutôt que contourné — décision explicitement validée par l'utilisateur (`docs/BACKLOG_METIER.md` Décision 2). `CompanyType`/`Role`/`OperationalRole` restent non résolus (hors périmètre de ce lot). « Aucun écran frontend pour les commandes/budgets/jalons » (reconduit depuis les Lots 1/5/6/4 respectivement) est désormais résolu : voir « Code — Lot 11 » ci-dessus.

**Écarts transversaux résolus au Lot 12 :** « Aucun écran frontend pour les charges/le tableau de bord/le reporting/les imports » (reconduit depuis les Lots 5, 6 et le Lot 1 respectivement) est désormais résolu : voir « Code — Lot 12 » ci-dessus. Ce lot clôt la phase « Frontend complet (V1) » (Lots 7 à 12) — tous les écrans métier listés par `docs/ROADMAP.md` pour cette phase sont désormais livrés.

**Écarts transversaux résolus au Lot 13 :** « Authentification de démonstration — toujours partielle » (Lot 0, reconduit jusqu'au Lot 12) est désormais résolu pour l'axe session : une session serveur réelle existe (`UserSession`, cookie HttpOnly), toujours sans mot de passe ni JWT (conforme au cahier des charges §6.5). « Rôles et permissions — toujours partiel » (Lot 0) est désormais résolu pour la combinaison rôle + permissions complémentaires (modèle RBAC, `RolePermission`/`UserPermission.Effect`/`PermissionResolutionService`) — seul l'axe périmètre organisationnel (département/service/équipe/propriété de la donnée) reste ouvert, explicitement reporté (voir ci-dessous). « Aucun CI/CD » (jamais formellement documenté comme écart, mais absent depuis le Lot 0) est résolu : `.github/workflows/ci.yml`.

**Nouveaux écarts constatés au Lot 13 :**

- **Périmètre organisationnel toujours absent** (écart reconduit depuis les Lots 2 à 12, le plus ancien du projet) : explicitement différé à un lot dédié après validation avec le Product Owner (`docs/BACKLOG_METIER.md` §17, Décision 3) — aucune règle partielle introduite pour ne jamais donner un faux sentiment de sécurité. **Traité au Lot 15** (voir `docs/ROADMAP.md`, roadmap figée jusqu'à la V1).
- **`CompanyType`/`Role`/`OperationalRole` toujours sans endpoint `GET`** (reconduit depuis les Lots 8/10/11) : hors périmètre de ce lot.
- **Extension de `[RequirePermission]` aux ~29 contrôleurs non gardés aujourd'hui non traitée** : seul le mécanisme de session/RBAC a évolué ; quels contrôleurs supplémentaires devraient porter une garde de permission reste une question métier proche du périmètre organisationnel, non tranchée ce lot.
- **`User.AccesGlobal` toujours non utilisé** : son usage naturel (contournement du périmètre organisationnel) n'a de sens qu'une fois ce périmètre construit — non traité tant que la Décision 3 ci-dessus reste différée.
- **Pas de nettoyage des sessions expirées** (simplification assumée à l'ouverture du lot) : filtrage à la lecture uniquement (`ExpiresAt`/`RevokedAt`), aucune tâche de fond — à reconsidérer si le volume de sessions devient significatif.
- **`RequirePermissionAttribute` non migré vers les policies natives ASP.NET Core** : `docs/ARCHITECTURE.md` §4 est corrigé pour refléter l'implémentation réelle (filtre MVC custom) plutôt que l'inverse — la migration elle-même reste une question ouverte, non tranchée.
- **SonarQube/SonarCloud, Docker, déploiement DEV/QUAL/PROD non construits** (décision explicite de l'utilisateur pour ce lot) : structure du pipeline CI pensée pour les accueillir sans refonte (`workflow_call`, jobs séparés) — voir `.github/workflows/ci.yml`.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : la migration `AddLot13AuthenticationRbac` est compilée pour les deux providers (design-time), seule SQLite est validée par une application réelle (base de développement existante, migrée à chaud).
- **Pas de bascule "se souvenir de moi" exposée à l'écran** (`UserSession.IsPersistent` toujours `false` à la création) : le modèle est prêt, aucune case à cocher construite — décision actée à l'ouverture du lot (§6.1, Décision 1).
- **Optimisation du bundle frontend toujours différée** (écart reconduit depuis les Lots 11/12, avertissement de taille non bloquant) : non demandée dans le périmètre validé de ce lot.

**Nouveaux écarts constatés au Lot 12 :**

- **`CompanyType`/`Role`/`OperationalRole` toujours sans endpoint `GET`** (reconduit depuis les Lots 8/10/11) : hors périmètre de ce lot, aucune décision de les traiter n'a été prise.
- **Restriction par périmètre organisationnel toujours absente** (écart reconduit depuis les Lots 2 à 11) : les nouveaux filtres/écrans du Lot 12 n'introduisent aucune nouvelle règle de sécurité serveur par département/service/équipe.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : aucune nouvelle migration ce lot (extensions de DTO/service sur des entités déjà existantes).
- **Désaccord de port `vite.config.ts`/`launchSettings.json` résolu avant clôture du lot** (reconduit depuis les Lots 9 et 11, signalé une troisième fois à l'utilisateur en fin de développement de ce lot) : `vite.config.ts` pointait `http://localhost:5297` sans qu'aucun script, test ou document du dépôt ne référence ce port ; `launchSettings.json` (seul fichier de référence réel, deux profils `http`/`https` cohérents sur `5215`) et `appsettings.Development.json` (`Cors.AllowedOrigins` attend le frontend sur `5173`, le port par défaut de Vite) corroboraient tous deux `5215` côté API. Le proxy est désormais aligné sur `5215` — `dotnet run` et `npm run dev` sans aucun paramètre fonctionnent ensemble sans contournement manuel. Correction de configuration pure, aucun comportement métier modifié.
- **Assistant de correspondance interactif des imports (§27.3 étape 5) toujours différé** (décision reconduite depuis le Lot 6, confirmée à l'ouverture de ce lot, Décision 4) : les en-têtes CSV doivent toujours correspondre exactement aux noms de propriété du DTO cible.

**Nouveaux écarts constatés au Lot 11 :**

- **Modèle Budget détaillé par ligne (§2, `docs/BACKLOG_METIER.md`) toujours non construit** : décision explicitement actée à l'ouverture du lot (Décision 1) de rester sur le modèle `Budget`/`BudgetVersion` existant — reporté à un futur lot dédié à cette réconciliation.
- **`CompanyType`/`Role`/`OperationalRole` toujours sans endpoint `GET`** (reconduit depuis les Lots 8/10) : hors périmètre de ce lot, aucune décision de les traiter n'a été prise.
- **`CalendarView` (composant transverse anticipé par `docs/ARCHITECTURE.md` §3, écart reconduit depuis le Lot 9) toujours non extrait** : le calendrier de `/jalons` est un second consommateur potentiel (après `/disponibilites`), mais implémenté directement dans `MilestonesListPage.tsx` plutôt que comme un composant partagé — `AvailabilityPage.tsx` (Lot 9) n'a volontairement pas été modifié ce lot (consigne explicite de ne pas rouvrir les lots précédents sauf strict nécessaire). L'extraction reste une opportunité de nettoyage future, à réévaluer si un troisième écran calendaire apparaît.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : aucune migration nouvelle ce lot (3 évolutions de filtre/agrégation sur des entités/actions déjà existantes).
- **Restriction par périmètre organisationnel toujours absente** (écart reconduit depuis les Lots 2 à 10) : les nouveaux filtres/écrans du Lot 11 n'introduisent aucune nouvelle règle de sécurité serveur par département/service/équipe.

**Nouveaux écarts constatés au Lot 10 :**

- **`OperationalRole` toujours sans endpoint `GET`** (même nature que `CompanyType`/`Role`, Lot 8) : contourné via `knownReferentials.ts` (`OPERATIONAL_ROLE_OPTIONS`), pas un vrai référentiel — décision de ne pas étendre la portée de la Décision 10 (ProjectStatus) à ce référentiel non discuté avec l'utilisateur.
- **`ProjectParticipant` toujours sans `Update`** (Create + Remove/désactivation uniquement, décision validée à l'ouverture du lot, Décision 4) : changer le rôle/la capacité/la période d'un participant après création nécessite un retrait puis un nouvel ajout.
- **Coût réel/contractuel/différentiel et reste-à-faire par participant (§17.2) toujours absents du backend** : seul le TJM applicable est calculé (`ProjectParticipantFinancialSummaryDto`). Le temps consommé par participant affiché à l'écran est une agrégation frontend sur des saisies déjà filtrées par permission (décision validée, Décision 5) — pas une nouvelle règle serveur, mais le « reste à faire » et les coûts par participant restent donc dérivés côté frontend (capacité prévue − consommé), jamais un montant financier calculé sans permission.
- **« Avancement » (%) jamais affiché qu'à « — »** : aucune formule n'est définie par le cahier des charges (§16.2 le liste comme champ, §29 ne le calcule pas) — décision actée à l'ouverture du lot (Décision 6) de ne jamais l'inventer côté frontend ni serveur. À définir dans un futur lot si une formule est validée avec le Product Owner.
- **« Commandes liées » (§16.2) non affichées en liste**, uniquement dérivables en fiche détail via `Budget.OrderId`/`ProjectParticipant.DefaultOrderId` (décision validée, Décision 7) — pas de colonne dédiée sur `/projets`, calcul non trivial à l'échelle d'une liste paginée.
- **Gantt non implémenté** (décision validée, Décision 8) : seuls `Timeline` (axe chronologique simple) et `WeeklyPlanningGrid` (grille de présentation) ont été construits, conformément à `docs/ROADMAP.md` qui ne nomme que ces deux composants pour ce lot. Reste au backlog, à valider explicitement avant un lot futur.
- **Sous-charge (§29.6) non filtrable sur `/planning-projet`** : exige un seuil configurable absent de `Settings` — même écart que `AvailabilityResultDto` (Lot 3/9). Seule la surcharge (comparaison directe charge/capacité, sans seuil) est filtrable.
- **Jeu de données de démonstration toujours en deçà du minimum du §35 sur d'autres axes** (80 saisies de temps, 20 références INC/CHG/PRB/RITM, 10 absences, 5 sociétés) : seuls les axes Projets (8) et Jalons (15) ont été portés au minimum ce lot, conformément à la demande explicite de l'utilisateur (Décision 9) — les autres axes restent au niveau des Lots 1-3, non enrichis (hors périmètre de ce lot).
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : la migration `AddLot10ProjectsSeedAndPlanning` est générée et compilable pour les deux providers (design-time), seule SQLite est validée par une application réelle de migration (base vide, redémarrage à froid).

**Nouveaux écarts constatés au Lot 9 :**

- **`TimeEntry` toujours sans `ApplicationId`** (`docs/BACKLOG_METIER.md` §11, 🔎 À clarifier) : écart antérieur au Lot 9 (introduit au Lot 3, jamais documenté avant l'analyse d'ouverture de ce lot), volontairement non corrigé — le formulaire Saisie des temps n'expose ni sélecteur ni filtre « par application ». Une correction relèverait d'un lot dédié au modèle `TimeEntry`.
- **Capacité par ressource calculée en N+1** (un appel `GET /resources/{id}/availability` par ressource affichée dans le tableau « Capacité par ressource », `/disponibilites`) : décision actée avec l'utilisateur à l'ouverture du lot (3.5), acceptable au périmètre actuel (13 ressources de démonstration) — un endpoint d'agrégation multi-ressources dédié n'a pas été construit pour ne pas anticiper un besoin de performance non exprimé (`CLAUDE.md` §5). À réévaluer si le nombre de ressources grandit significativement.
- **`AvailabilityResultDto` (Lot 3) n'expose ni « disponibilités restantes » ni « surcharges »** (§22.2, §29.6) : seuls capacité théorique/réelle, taux de disponibilité, jours fériés/absence et charge RUN/hors RUN sont exposés. « Disponibilités restantes » (capacité réelle − charge consommée) et « surcharge »/« sous-charge » (§29.6, qui exige des seuils configurables absents de `Settings`) ne sont ni calculés côté backend ni affichés sur `/disponibilites`. Écart antérieur au Lot 9 (Lot 3), non corrigé ici : l'ajouter suppose une évolution du DTO/service de capacité et la définition d'un seuil paramétrable, hors périmètre frontend-only sans validation préalable (`CLAUDE.md` §5).
- **`CalendarView` (composant transverse anticipé par `docs/ARCHITECTURE.md` §3) non introduit** : le calendrier coloré de `/disponibilites` est implémenté directement dans `AvailabilityPage.tsx` plutôt que comme un composant partagé — un seul écran en a besoin à ce stade, extraire une abstraction pour un unique consommateur aurait anticipé un besoin non encore exprimé (`CLAUDE.md` §5). À reconsidérer si un futur écran (Lot 10-12) a besoin d'un rendu calendaire similaire.
- **Restriction par périmètre organisationnel toujours absente** (écart reconduit depuis les Lots 2 à 8) : les filtres département/service/équipe de `/disponibilites` restent des filtres d'affichage côté frontend sur des listes déjà entièrement accessibles à toute identité — aucune nouvelle règle de sécurité serveur introduite ce lot (décision actée 3.6, cf. « Décisions actées »).
- **Découverte d'environnement local, non liée au code du lot** : `frontend/safran-time-tracker-web/vite.config.ts` (proxy `/api` → `http://localhost:5297`) et `backend/SafranTimeTracker.Api/Properties/launchSettings.json` (profils `http`/`https` → port `5215`) sont en désaccord — préexistant, non introduit par ce lot (`vite.config.ts` inchangé depuis le Lot 7). La validation visuelle de ce lot a démarré l'API avec `--urls http://localhost:5297 --no-launch-profile` (+ `ASPNETCORE_ENVIRONMENT=Development` explicite, `--no-launch-profile` ne chargeant pas les variables d'environnement du profil) pour faire correspondre le port attendu par le proxy Vite. Non corrigé dans ce lot (fichiers partagés, hors périmètre fonctionnel) — à trancher explicitement avant un prochain lot frontend pour éviter de refaire ce contournement.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : aucune nouvelle migration ce lot (filtres de requête + une action `PUT` sur colonnes/entité déjà existantes).

**Nouveaux écarts constatés au Lot 8 :**

- **Deux référentiels backend sans endpoint `GET` : `CompanyType` et `Role`.** Ni l'un ni l'autre n'a jamais eu de contrôleur dédié (depuis le Lot 1) ; seules leurs valeurs seedées existent (`SeedIds.CompanyTypeInterne/Externe`, `SeedIds.RoleIngenieur/…`). Contournement documenté et centralisé en un seul endroit (`frontend/src/lib/knownReferentials.ts`) : les identifiants seedés sont portés en dur côté frontend pour peupler le sélecteur de type de société (formulaire Société) et l'affichage du rôle (fiche Ressource, section Sécurité), à remplacer par de vrais référentiels dès que les endpoints existeront. Ne s'applique qu'à l'affichage/la sélection, jamais à une règle de sécurité : la garde d'accès réelle reste côté serveur (CLAUDE.md §17).
- **Gestion des utilisateurs limitée à activer/désactiver dans le panneau Administration**, alors que l'API expose déjà la modification de rôle et l'octroi/retrait de permission par utilisateur (`UserService`, Lot 6). L'écran dédié (sélecteur de rôle, cases à cocher de permissions, historique d'audit associé) n'a pas été construit ce lot pour ne pas dépasser le périmètre demandé — l'API le permettrait sans évolution backend supplémentaire.
- **Onglet Commandes en lecture seule** (référence, libellé, dates — aucun montant). La gestion complète (machine d'état, rallonges, réceptions) est explicitement le périmètre du Lot 11 — Commandes, Budgets et Jalons (`docs/ROADMAP.md`) ; construire plus ici aurait anticipé un lot ultérieur (CLAUDE.md §20).
- **Domaine « Catégories » non construit.** Évoqué dans le brief initial, reporté par l'utilisateur faute de définition précise (catégorie de projet ? de dépense ? d'application ?) — aucune ligne n'a été ajoutée à `docs/BACKLOG_METIER.md`, ce n'est donc pas une règle validée en attente mais une piste explicitement non retenue pour ce lot.
- **`npm run format:check` signale la quasi-totalité du dépôt** (97 fichiers, y compris des fichiers non touchés par ce lot comme `App.tsx`/`main.tsx`/`vite.config.ts`) sur cette copie de travail Windows : confirmé pré-existant (reproduit après `git stash` des changements du lot) et causé par `core.autocrlf=true` convertissant tout le dépôt en CRLF au checkout, alors que Prettier attend du LF. N'est pas un écart introduit par le Lot 8 ; corriger nécessiterait une décision de configuration Git dépôt entier (`.gitattributes`), hors périmètre d'un lot fonctionnel.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : la migration `AddLot8ReferentialsAndAdministration` est générée et compilable pour les deux providers (design-time), seule SQLite est validée par une application réelle de migration.
- **Restriction par périmètre organisationnel toujours absente** (écart reconduit depuis les Lots 2 à 7) : les nouveaux référentiels du Lot 8 sont volontairement ouverts (aucune permission requise), cohérent avec les référentiels non financiers existants (`departments`, `activity-types`, …).

**Nouveaux écarts constatés au Lot 7 :**

- **Aucun écran métier livré, par construction du lot.** Les 16 entrées de sidebar rendent toutes un écran neutre « à venir » (`PlaceholderPage`) : aucune donnée de `Temps`, `Projets`, `Commandes`, `Budgets`, `Charges`, etc. n'est encore affichée, malgré une API complète depuis les Lots 1 à 6. Ce n'est pas un écart mais le périmètre explicitement validé du lot — les écrans métier arrivent lot par lot de Lot 8 à Lot 12 (`docs/ROADMAP.md`).
- **Filtrage de la sidebar par droits limité à 2 entrées sur 16** (`Budgets`, `Imports`) : les 14 autres restent toujours visibles faute de garde serveur de niveau contrôleur non ambiguë à ce jour (ex. `Administration` regroupera 13 sous-onglets aux permissions hétérogènes à partir du Lot 8, `AuditLogsController` est gardé par `AUDIT_VIEW` mais n'est pas lui-même une entrée de sidebar). Se resserre naturellement à mesure que les lots suivants ajoutent des gardes serveur dédiées.
- **Restriction par périmètre organisationnel toujours absente** (écart reconduit depuis les Lots 2 à 6) : `PermissionGuard` reflète uniquement l'axe permission déjà appliqué côté serveur, pas de notion de département/service/équipe.
- **Poids du bundle JavaScript non optimisé** (~626 Ko avant gzip, ~204 Ko après) : MUI + `x-data-grid` + `x-date-pickers` + `dayjs` chargés en un seul chunk, aucun découpage (`dynamic import`) mis en place. Optimisation explicitement hors périmètre de ce lot, prévue au Lot 13 (Industrialisation, `docs/ROADMAP.md`).
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : aucune migration nouvelle ce lot (seul ajout backend : `GET /api/v1/permissions`, sans nouvelle entité ni migration).

**Nouveaux écarts constatés au Lot 6 :**

- **Aucun écran frontend pour les imports, l'audit, les réceptions de commande ou l'administration utilisateur.** Comme à tous les lots précédents, seule l'API (contrôleurs + Swagger) est livrée. Le mapping de colonnes de l'assistant d'import (§27.3 étape 5) est en particulier un écran à part entière (glisser-déposer de colonnes, aperçu tabulaire) qui n'existe nulle part : le MVP s'appuie sur des en-têtes CSV correspondant exactement aux noms de propriété (`GET /api/v1/imports/types`), décision validée par l'utilisateur à l'ouverture du lot.
- **Modes d'import limités par type, strictement dérivés des services métier existants.** 5 des 16 types (Resources, Applications, Organisation, Absences, ProjectParticipants) ne supportent que l'Ajout, faute de méthode de mise à jour déjà exposée par leur service ; aucune n'a été ajoutée uniquement pour satisfaire l'import (règle validée à l'ouverture du lot — voir « Code — Lot 6 » pour le détail par type). Étendre un type nécessite d'abord d'ajouter la méthode de service correspondante (Update/Archive), pas de modifier le moteur d'import lui-même.
- **Mode Complet non disponible pour les types scopés à un parent** (`ProjectParticipants`, `Plannings`) : le moteur d'import n'a pas de notion de « périmètre de fichier », un fichier ne décrivant qu'un seul projet ne doit pas archiver silencieusement les participants/plannings des autres projets. Limitation documentée plutôt que devinée ; une évolution future demanderait de faire porter un filtre de périmètre explicite au niveau du lot d'import.
- **Comparaison SharePoint simulée limitée à l'état courant de la base**, pas un stockage fichier-à-fichier (§27.4) — décision validée par l'utilisateur, réutilise le même moteur de diff que les imports CSV classiques plutôt que d'introduire un second mécanisme de comparaison.
- **Restriction par périmètre organisationnel toujours absente** pour `USER_ADMINISTRATION`/`IMPORT_EXECUTE`/`AUDIT_VIEW`/`ORDER_RECEIPT_OVERRIDE` (même écart reconduit depuis les Lots 2 à 5 pour les permissions existantes) : seul l'axe permission est appliqué (`RequirePermissionAttribute`), la combinaison avec le périmètre organisationnel reste à construire (CLAUDE.md §10).
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : la migration `AddLot6ImportsAndAudit` est générée et compilable pour les deux providers (design-time), mais seule SQLite est validée par une application réelle de migration.
- **Décision de modélisation à noter (pas un écart fonctionnel) :** le garde-fou « dernier administrateur actif » (CLAUDE.md §17) est appliqué comme un invariant système (aucun autre Administrateur actif ne peut tomber à zéro), pas seulement pour l'auto-retrait explicitement cité par le cahier des charges — plus strict que le texte littéral, jamais moins.

**Écarts hérités du Lot 0 — statut au Lot 5 :**

Trois éléments listés dans la description du Lot 0 de `docs/ROADMAP.md` avaient été volontairement écartés à la clôture du Lot 0. Statut réévalué après le Lot 5 :

- **Authentification de démonstration** — **toujours partielle, inchangée au Lot 5.** `DemoCurrentUserProvider` (`ICurrentUser`) résout l'appelant depuis l'en-tête `X-Demo-User`, vérifié en base — **volontairement pas** un login, un JWT, une session, un cookie ou ASP.NET Identity (contrainte explicite actée au Lot 2). Les services `Order`/`OrderExtension`/`Budget`/`Reporting`/`Export`/`DashboardKpi` du Lot 5 utilisent tous `ICurrentUser.Identifier` pour `CreatedBy`/`UpdatedBy`/`GeneratedBy`, comme les lots précédents.
- **Rôles et permissions** — **toujours partiel.** Aucun endpoint de gestion des rôles/permissions n'existe. Le Lot 5 ajoute une seule permission ciblée (`TIME_ENTRY_CORRECTION`, §13.4) mais aucune restriction par périmètre organisationnel : n'importe quel appelant peut faire évoluer le statut d'une commande ou d'un budget, faute de périmètre organisationnel construit (écart déjà noté aux Lots 2 à 4).
- **Design system** — **toujours non mis en place.** Aucun écran fonctionnel n'a été ajouté côté frontend au Lot 5 (voir ci-dessous), y compris pour les tableaux de bord/graphiques que ce lot prépare côté API ; le sujet reste entier.

**Nouveaux écarts constatés au Lot 5 :**

- **Aucun écran frontend pour les budgets, rallonges, tableau de bord ou reporting.** Comme aux Lots 1 à 4, le lot a livré l'API (contrôleurs + Swagger) mais aucun composant React consommateur — plus sensible ici que pour les lots précédents, ce lot étant orienté « tableaux de bord/graphiques » (§21.3, §25.3), dont seules les données agrégées sont exposées, jamais un rendu graphique.
- **`Budget` limité à un projet et/ou une commande.** Le « périmètre de pilotage » libre évoqué au §14.1 (département/service/équipe, sans projet ni commande) n'est pas modélisé, faute d'entité « périmètre » dédiée dans l'inventaire minimum du §30 ; décision actée à la validation du plan.
- **« Commandes à renouveler » et « besoins de rallonge » (§26.2) simplifiés.** Fenêtre fixe de 30 jours avant l'échéance ajustée pour les renouvellements (aucun paramètre dédié au §28.2) ; « besoin de rallonge » assimilé à un reste financier négatif. Documenté en commentaire XML (`FinancialReportDto`).
- **Atterrissage financier MVP (§29.5/§14.3), formule simple validée par l'utilisateur.** `ProjectPlanningCalculator.CalculateAtterrissageFinancier` extrapole proportionnellement au facteur de la charge (ou retombe sur le coût réel consommé si aucune charge n'est renseignée) — pas de modèle prédictif indépendant (courbe de consommation, tendance historique, etc.).
- **`AuditLog` (Lot 6) toujours absent** : les transitions de statut de commande/budget, les rallonges et les ajustements de budget sont tracés uniquement via `CreatedAt`/`CreatedBy`/`UpdatedAt`/`UpdatedBy` (`AuditableEntity`) ou les entités append-only elles-mêmes (`OrderExtension`, `BudgetVersion`) — même écart qu'aux Lots 1 à 4, pour les mêmes raisons. `ExportLog` (Lot 5) est une entité dédiée et volontairement plus étroite : elle ne couvre que les exports (§26.3), pas l'audit transversal du §28.3.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : la migration `AddLot5BudgetsAndReporting` est générée et compilable pour les deux providers (design-time), mais seule SQLite est validée par une application réelle de migration.
- **Décision de modélisation à noter (pas un écart fonctionnel) :** deux tests de `BudgetsAndReportingTests` ont dû être réécrits en cours d'écriture pour créer leur propre budget/commande via l'API plutôt que de réutiliser les entités seedées partagées (« Migration ELM », `CMD-2026-0001`) — les tests d'une même classe partagent la même base SQLite (`IClassFixture`), donc un test qui modifie le statut ou le montant ajusté d'une entité seedée peut faire échouer un autre test de lecture de la même classe selon l'ordre d'exécution. Retenu comme convention pour les futurs tests de transitions d'état (voir `docs/IMPLEMENTATION_STATUS.md` — précédent déjà partiellement suivi au Lot 4 avec `PRJ-VTOM-2026` pour l'archivage/réactivation).

**Écarts constatés au Lot 4 (reconduits, statut réévalué après le Lot 5) :**

- **Références opérationnelles (§17.7) : synthèse désormais exposée.** `ReportingService.GetProjectLinkedReferencesAsync` (`GET /api/v1/reporting/projects/{id}/linked-references`) ferme l'écart — écart résolu.
- **« Risque budget » (§29.5) : formule littérale désormais appliquée.** `ProjectPlanningCalculator.CalculateBudgetRisk` compare l'atterrissage financier au budget **ajusté** (via le `Budget` lié au projet) — écart résolu. « Reste à faire » reste en revanche simplifié (voir écart Lot 3 ci-dessous, inchangé).
- **`MilestoneType` construit comme entité référentielle complète malgré son absence de la liste minimum du §30.** Justifié par le §24.1, qui exige explicitement que la classification des jalons soit administrable — même raisonnement que `ActivityType.IsRun` au Lot 3 ; décision actée à la validation du plan Lot 4, inchangée.
- **Pas de détection de cycle sur `Milestone.DependsOnMilestoneId`** (auto-référence optionnelle) : toujours non traité, hors périmètre du Lot 5.
- **Décision de modélisation à noter (pas un écart fonctionnel) :** `ProjectPlanVersionCreateRequest`/`ProjectPlanVersionAdjustmentRequest` — deux DTO distincts pour éviter une ambiguïté `IValidator<T>` en DI (FluentValidation), reconduit sans changement.

**Écarts constatés au Lot 3 (reconduits, statut réévalué après le Lot 5) :**

- **Aucun écran frontend pour le temps et la capacité.** Comme aux Lots 1 et 2, le lot a livré l'API (contrôleurs + Swagger) mais aucun composant React consommateur ; le frontend n'a subi aucune modification ce lot.
- **Recalcul explicite audité (§19.6) différé au Lot 6**, conformément au plan validé : `ITimeEntryRevaluationService` (interface + implémentation `NotImplementedException`) est posée et enregistrée en DI pour qu'aucun contrôleur, service ou règle de sécurité n'ait à changer lors de son implémentation réelle, qui dépend d'`AuditLog` (permission dédiée, confirmation, motif, conservation de l'ancienne valeur en audit).
- **`AuditLog` (Lot 6) toujours absent** : les corrections de saisies de temps (`PUT /time-entries/{id}`) et les décisions d'absence sont tracées uniquement via `UpdatedAt`/`UpdatedBy` (`AuditableEntity`) — même écart qu'aux Lots 1 et 2, pour les mêmes raisons.
- **PostgreSQL et SQL Server toujours non testés en conditions réelles** (reconduction de l'écart du Lot 0) : la migration `AddLot3TimeAndCapacity` est générée et compilable pour les deux providers (design-time), mais seule SQLite est validée par une application réelle de migration.
- **Simplifications de modélisation par rapport à une lecture littérale du §19.1/§22/§29, documentées en commentaire XML dans le code :**
  - Le champ « utilisateur » de la saisie de temps (§19.1, distinct de « ressource ») n'est pas modélisé séparément : `TimeEntry.ResourceId` porte la personne concernée, `CreatedBy`/`UpdatedBy` (`ICurrentUser`) portent l'auteur réel — un champ dédié serait redondant dans le cas courant et le cahier ne précise pas de cas où il diffère.
  - Le champ « semaine » (§19.1) n'est pas stocké : calculé à la volée depuis `Date` pour le filtre (§19.4), pour éviter une donnée dérivée pouvant devenir incohérente.
  - « Indisponibilités » (§29.2) n'a pas de mécanisme dédié : couvert par `AbsenceType.Indisponible`, un type d'absence validée comme un autre.
  - « Jour ouvré » (§29.1) est interprété comme lundi-vendredi (cohérent avec `Settings.JoursOuvresParSemaine = 5` par défaut), pas un calendrier de jours ouvrés paramétrable par ressource/pays.
  - `DashboardKpi` (§30, catégorie « Paramétrage ») **différé** : aucun lien avec « saisie des temps/absences/disponibilités/calculs capacité/charges RUN-hors RUN » (ROADMAP Lot 3), relève du tableau de bord (§25, plutôt Lot 5).

**Écarts constatés au Lot 2 (reconduits, statut réévalué après le Lot 3) :**

- **`TimeEntryFinancialSnapshot` : existe désormais** (voir « Code — Lot 3 » ci-dessus) — écart résolu.
- **`sourceTjmPersonne`/`sourceContrat` : renseignés désormais** dans `FinancialCalculationResultDto` et persistés dans le snapshot (valeur constante "ResourceTjmHistory"/"CompanyContractHistory", une seule source existant à ce jour) — écart résolu.
- **`AuditLog` (Lot 6) toujours absent** pour les corrections de TJM/contrat/rattachement — écart reconduit, inchangé au Lot 3 (hors périmètre).
- **Autorisation via `RequirePermissionAttribute`** — choix reconduit sans changement au Lot 3 (voir « Décisions actées » du Lot 2).

**Écarts constatés au Lot 1 (reconduits, statut réévalué après le Lot 5) :**

- **`ResourceCapacityPeriod` : existe désormais** (voir « Code — Lot 3 » ci-dessus) — écart résolu.
- **`MilestoneType` : existe désormais** (voir « Code — Lot 4 » ci-dessus) — écart résolu. (`HolidayCalendar` et `ActivityType` existent depuis le Lot 3 ; `AbsenceType` ne figure en réalité **pas** dans la liste minimum du §30 — l'écart Lot 1 l'annonçait à tort, corrigé au Lot 3.)
- **`Order` : lien direct vers `Project`, budget/dates ajustés et rallonges désormais présents** (`Order.ProjectId`, `BudgetFinancierAjuste`, `BudgetJoursAjuste`, `DateFinAjustee`, `OrderExtension` — voir « Code — Lot 5 » ci-dessus) — écart résolu. Consommation en jours et coûts réel/contractuel/différentiel de la commande sont désormais exposés dans `OrderDto.FinancialSummary` (agrégats de saisies valorisées, §20.6) — écart résolu.
- Point encore différé :
  - `ApplicationReference` : charge RUN/hors RUN, compteurs INC/CHG/PRB/RITM, projets associés — tous **techniquement calculables** dès à présent (`TimeEntry`, `Project.ApplicationId`) via les endpoints de reporting génériques du Lot 5 (`ReportingService`, filtrables par `applicationId`), mais aucun endpoint d'agrégation dédié à une fiche application n'est encore livré.
- **Décision de modélisation à noter (pas un écart fonctionnel) :** la section « Organisation » de la fiche utilisateur (cahier des charges §10.2 : département, service, équipe, société courante, commande par défaut, capacité) est portée par `Resource` et non dupliquée sur `User`, conformément à la distinction utilisateur/ressource actée au §10.1 (`User` = compte de connexion, `Resource` = personne planifiable). L'information reste accessible via `User.ResourceId`.

**Autres écarts / limites techniques constatés au Lot 0 (reconduits) :**

- **Scripts PowerShell incomplets par rapport à `CLAUDE.md` §19.** Seuls `install.ps1`, `build.ps1` et `publish.ps1` existent. `deploy.ps1`, `rollback.ps1`, `backup-database.ps1`, `restore-database.ps1`, `health-check.ps1`, `install-iis.ps1`, `purge-logs.ps1` restent à créer — ils supposent une cible de déploiement (serveur, base) qui n'existe pas encore. Non traité aux Lots 1, 2 et 3 (hors périmètre de chacun).
- **Entité `Application` renommée en `ApplicationReference`** (cahier des charges §15, §30) et service `ApplicationService` renommé `ApplicationReferenceService` (cahier des charges §31) — décision actée à la clôture de la phase documentaire pour éviter la collision avec le namespace `SafranTimeTracker.Application`. Vocabulaire fonctionnel inchangé. Appliquée de façon cohérente aux Lots 1 et 2 (`ApplicationReferenceService`, `ApplicationsController`).

## Décisions actées

| Décision | Choix retenu | Date |
|---|---|---|
| Version .NET LTS | .NET 10 (SDK 10.0.302 testé) | 2026-07-15 |
| Mapping DTO ↔ entité | Mapster | 2026-07-15 |
| Mocking tests backend | NSubstitute | 2026-07-15 |
| Bibliothèque de graphiques frontend | Recharts | 2026-07-15 |
| Document `docs/DEPLOYMENT_WINDOWS.md` séparé | Non créé — contenu conservé dans `CLAUDE.md` §18-19 | 2026-07-15 |
| Collision entité `Application` / namespace `SafranTimeTracker.Application` | Entité renommée `ApplicationReference`, service `ApplicationReferenceService` | 2026-07-15 |
| Format de solution .NET | `.slnx` (nouveau format XML, par défaut sur le SDK .NET 10) | 2026-07-15 |
| Architecture des migrations EF Core multi-providers | Un projet de migrations dédié par provider sous `database/{provider}/` (pattern officiel Microsoft), plutôt qu'un assembly unique | 2026-07-15 |
| Version FluentAssertions | Épinglée à `7.2.2` (dernière version sous licence Apache 2.0) — la v8+ exige une licence commerciale payante (changement de licence Xceed, janvier 2025), incompatible avec un usage interne sans achat de licence | 2026-07-15 |
| Vulnérabilité `SQLitePCLRaw.lib.e_sqlite3` (NU1903, GHSA-2m69-gcr7-jv3q) | Épinglage explicite de `SQLitePCLRaw.bundle_e_sqlite3` en `2.1.12` (version corrigée) dans tous les projets référençant le provider SQLite | 2026-07-15 |
| Linter frontend | ESLint (flat config) + Prettier, à la place d'`oxlint` (nouveau défaut du template Vite) — conforme au choix déjà documenté dans `docs/CONVENTIONS.md` §9 | 2026-07-15 |
| Persistance Lot 1 | `EfRepository<T>` générique unique (`IRepository<T>`/`IReadRepository<T>`), pas de dépôt dédié par entité — le Lot 1 ne contient aucune règle de recherche métier complexe qui le justifierait | 2026-07-15 |
| Enregistrement des validateurs FluentValidation | `FluentValidation.DependencyInjectionExtensions` (scan automatique de l'assembly `Application`) plutôt qu'un enregistrement manuel par validateur | 2026-07-15 |
| Bootstrap logger Serilog dans `Program.cs` | Retiré (motif technique, pas fonctionnel) : le `Log.Logger` statique à deux phases entre en conflit avec `WebApplicationFactory`, qui réexécute `Program` à chaque instanciation d'hôte de test (« logger already frozen ») ; la configuration Serilog passe désormais uniquement par `builder.Host.UseSerilog(...)` | 2026-07-15 |
| Rattachement organisationnel de la fiche utilisateur (§10.2) | Porté par `Resource` (département, service, équipe, société courante, commande par défaut, capacité), pas dupliqué sur `User`, pour respecter la distinction utilisateur/ressource du §10.1 | 2026-07-15 |
| Application réelle des migrations dans les tests d'intégration | Le projet `SafranTimeTracker.Tests` référence directement `SafranTimeTracker.Migrations.Sqlite` (`ProjectReference`) pour que `WebApplicationFactory` applique les migrations SQLite réelles au démarrage de chaque test, plutôt qu'un simple `EnsureCreated()` | 2026-07-15 |
| Identité de démonstration (Lot 2) | Abstraction `ICurrentUser` (Application) + implémentation `DemoCurrentUserProvider` (Api, en-tête `X-Demo-User` résolu contre `User`/`UserPermission` réels) — explicitement pas de login, JWT, session, cookie ni ASP.NET Identity (demande explicite) ; remplaçable par un provider LDAP/OIDC sans toucher aux contrôleurs, services ou règles de sécurité | 2026-07-15 |
| Garde de permission financière (Lot 2) | `RequirePermissionAttribute` (`IAsyncAuthorizationFilter` MVC) ne dépendant que d'`ICurrentUser`, plutôt que le système `Authorization`/`ClaimsPrincipal` natif d'ASP.NET Core, pour rester strictement découplé de toute authentification | 2026-07-15 |
| Conflits métier (chevauchement, concurrence) | `BusinessConflictException` + `IExceptionHandler` dédié → 409 ProblemDetails (CLAUDE.md §12), distinct des 400 FluentValidation (format/existence) | 2026-07-15 |
| Jeton de concurrence optimiste multi-provider | Propriété `Guid ConcurrencyStamp` gérée applicativement (`.IsConcurrencyToken()`, régénérée à chaque `UpdateAsync`) plutôt qu'un `rowversion`/`[Timestamp]` natif SQL Server, pour rester identique sur les 3 providers (`docs/DATABASE.md` §1) | 2026-07-15 |
| Rattachement société utilisé pour le calcul financier | `ResourceCompanyAssignment` (historisé, Lot 2) devient la source de vérité pour `FinancialCalculationService` ; `Resource.CompanyId` (Lot 1) reste un pointeur d'affichage "société courante" non historisé, jamais utilisé par le calcul | 2026-07-15 |
| Persistance du snapshot financier | Non persistée au Lot 2 (`TimeEntryFinancialSnapshot` référence `TimeEntry`, Lot 3) ; `FinancialCalculationService` expose un DTO de résultat calculé à la demande, réutilisable tel quel par le Lot 3 | 2026-07-15 |
| Métadonnées de validation de référence sur `ActivityType` (Lot 3, demande explicite) | `ReferenceRequired`/`ReferenceFormatRegex`/`ReferenceExample` portés par la donnée plutôt que codés en dur dans un validateur — un seul validateur (`TimeEntryCreateRequestValidator`/`UpdateRequestValidator`) sert tous les types d'activité présents et futurs | 2026-07-15 |
| Préparation du recalcul explicite (Lot 3, demande explicite) | `ITimeEntryRevaluationService` posée dès le Lot 3 (implémentation `NotImplementedException`, enregistrée en DI) pour que le Lot 6 (§19.6, dépend d'AuditLog) n'exige aucune refonte de contrôleur, service ou règle de sécurité | 2026-07-15 |
| Sous-objet financier omis au niveau champ (Lot 3) | `TimeEntryDto.FinancialSnapshot` projeté à `null` dans `TimeEntryService` sans `FINANCIAL_DATA_VIEW` (CLAUDE.md §13) — premier DTO mixte financier/non financier du projet, à la différence des endpoints intégralement financiers du Lot 2 gardés par `RequirePermissionAttribute` | 2026-07-15 |
| Clé du snapshot financier | `TimeEntryFinancialSnapshot.Id` réutilise `TimeEntryId` (clé partagée) plutôt qu'un `Guid` indépendant — corrige un bug de suivi EF Core découvert pendant les tests : une entité chargée via `IReadRepository.Query()` (`AsNoTracking`) ne peut pas être mutée puis sauvegardée par `SaveChangesAsync()`, contrairement à `GetByIdAsync()` (suivi) | 2026-07-15 |
| Workflow d'absence et paramètre désactivé (§23.3) | Soumission avec `Settings.ActivationValidationAbsences = false` vaut validation immédiate (transition directe vers Validé) plutôt qu'un statut intermédiaire non prévu par le cahier — `AvailabilityService` n'a alors besoin de filtrer que sur le statut Validé, sans connaître ce paramètre | 2026-07-15 |
| Règle de blocage « période close » (§19.4) | Réutilise `Settings.DelaiModificationTempsJours` (Lot 1) plutôt qu'une nouvelle entité de calendrier de clôture | 2026-07-15 |
| Cycle de vie des versions de planning (§18.3, précision actée avec l'utilisateur, Lot 4) | Une seule version Initiale par projet (immuable) ; plusieurs versions Ajustées, une seule Active à la fois ; la précédente Active bascule automatiquement à Archivée à la création d'une nouvelle, historique jamais supprimé | 2026-07-15 |
| Écarts et risques projet (§29.5, Lot 4) | Fonctions pures `ProjectPlanningCalculator` (aucun accès base de données), invoquées uniquement par `ProjectPlanningService` — même principe que `FinancialCalculationService` (Lot 2) et `AvailabilityService.GetApplicableDailyCapacity` (Lot 3) | 2026-07-15 |
| Réutilisation du calcul financier pour le TJM applicable d'un participant (Lot 4) | `ProjectParticipantService` appelle `FinancialCalculationService.CalculateAsync` (Lot 2, non modifié) à la date du jour avec `HeuresSaisies = 0`, plutôt que dupliquer la recherche du TJM/société applicables à une date | 2026-07-15 |
| DTO de création de version de planning (Lot 4) | Deux DTO de forme quasi identique (`ProjectPlanVersionCreateRequest` motif optionnel / `ProjectPlanVersionAdjustmentRequest` motif obligatoire) plutôt qu'un DTO unique, pour éviter une ambiguïté d'enregistrement `IValidator<T>` en DI (FluentValidation) entre les deux règles de validation | 2026-07-15 |
| `MilestoneType` en entité référentielle complète (Lot 4, demande explicite) | Construit comme `ActivityType` (Lot 3) malgré son absence de la liste minimum du §30, car le §24.1 exige explicitement que la classification des jalons soit administrable | 2026-07-15 |
| Références opérationnelles INC/CHG/PRB/RITM/VABE/VSR liées à un projet (§17.7, Lot 4) | Décision actée à la validation du plan : donnée déjà portée par `TimeEntry.ProjectId`/`TimeEntry.Reference`, mais aucun endpoint de synthèse dédié livré ce lot — différé (Lot 5 ou synthèse dédiée ultérieure) — **résolu au Lot 5** | 2026-07-15 |
| Bibliothèques d'export réel (Lot 5, décisions validées par l'utilisateur) | CSV natif (`System.Text`, aucune dépendance) ; Excel via **ClosedXML** (licence MIT) ; PDF via **QuestPDF** (licence Community) | 2026-07-15 |
| Machine d'état des commandes (§13.2, précision actée avec l'utilisateur, Lot 5) | Transitions dédiées (`activate`/`suspend`/`mark-consumed`/`close`) plutôt qu'un champ statut libre ; aucune ne permet de sortir de Clôturée — seule `reopen` (motif obligatoire) le permet, jamais une réouverture implicite | 2026-07-15 |
| Blocage de saisie sur commande clôturée (§13.4, Lot 5) | Nouvelle permission dédiée `TIME_ENTRY_CORRECTION` plutôt que réutiliser `FINANCIAL_DATA_VIEW` — axe de permission indépendant, cohérent avec le modèle à permissions complémentaires du §6.1 | 2026-07-15 |
| Formule d'atterrissage financier MVP (§29.5/§14.3, validée par l'utilisateur, Lot 5) | Extrapolation proportionnelle sur le facteur de la charge (atterrissageCharge/chargeConsommée × coûtRéelConsommé), repli sur le coût réel consommé si aucune charge n'est renseignée — réutilisée telle quelle par `BudgetService` pour éviter de dupliquer la règle « risque budget » entre projet et budget générique | 2026-07-15 |
| `Budget` distinct du budget propre à `Order` (Lot 5) | `Budget`/`BudgetVersion` (§14) est l'objet de pilotage transverse lié à un projet et/ou une commande, avec son propre historique — distinct du budget ajusté propre à `Order` (§13.2, augmenté par `OrderExtension`, §13.3) | 2026-07-15 |
| `ExportLog` plutôt qu'anticiper l'`AuditLog` général (§26.3, Lot 5) | Entité légère et dédiée, ne couvre que les exports (date, auteur, version, filtres, format) — ne préempte pas la conception de l'`AuditLog` transversal du Lot 6 (§28.3) | 2026-07-15 |
| Version de l'application dans les exports (§26.3 "date et auteur de l'export", Lot 5) | Lue via `Assembly.GetName().Version`, dérivée de `<Version>` dans `Directory.Build.props` (racine), tenue en cohérence avec les tags Git (`v0.5.0`, ...) plutôt qu'une constante en dur dans le code | 2026-07-15 |
| Rendu d'export générique (Lot 5) | `ReportingTableDto` (titre, colonnes, lignes) produit exclusivement par `ReportingService`, restitué par `ExportService` en CSV/Excel/PDF — un seul rendu par format, quel que soit le rapport source, jamais de logique métier dans `ExportService` (demande explicite de l'utilisateur : calculs cantonnés à `BudgetService`/`ReportingService`/`ProjectPlanningService`/`FinancialCalculationService`) | 2026-07-15 |
| Isolation des tests de transition d'état partageant une entité seedée (Lot 5) | Un test qui mute le statut/montant d'une entité seedée partagée (budget, commande) crée sa propre entité via l'API plutôt que de réutiliser celle du seed, pour ne pas interférer avec un autre test de lecture de la même classe (base SQLite partagée par `IClassFixture`) | 2026-07-15 |
| Pattern d'écriture `AuditService` (Lot 6) | `RecordAsync` ajoute l'entrée au suivi EF Core sans appeler `SaveChangesAsync` — c'est le `SaveChangesAsync` du service métier appelant qui persiste les deux ensembles dans une seule transaction, sans introduire de Unit of Work dédiée | 2026-07-17 |
| Recalcul explicite d'un instantané financier (§19.6, Lot 6) | La relation 1-1 à clé partagée `TimeEntryFinancialSnapshot`/`TimeEntry` (CLAUDE.md §11) ne permet pas une seconde ligne de snapshot : "créer un nouveau snapshot" se traduit par le remplacement des valeurs de l'instantané existant, l'ancienne valeur étant intégralement conservée dans `AuditLog.OldValue` (§19.6 "conservation des anciennes valeurs dans l'audit") | 2026-07-17 |
| Workflow de commande "Demande d'achat → Commande → Réceptions partielles → Clôture" (règle métier validée, Lot 6) | Représenté par la machine d'état `Order`/`OrderStatus` **inchangée depuis le Lot 5** (pas de nouveau statut, pas de renommage, pour ne pas régresser un lot déjà tagué) complétée par une nouvelle entité append-only `OrderReceipt` (réceptions partielles répétables) | 2026-07-17 |
| Blocage d'une réception de commande au-delà du reste réceptionnable (règle métier validée, Lot 6) | Permission dédiée `ORDER_RECEIPT_OVERRIDE` plutôt qu'un simple booléen de dérogation sur la requête — même principe que `TIME_ENTRY_CORRECTION` (Lot 5) : bloqué par défaut (409), débloqué uniquement par une permission explicite | 2026-07-17 |
| Endpoints d'administration utilisateur manquants avant le Lot 6 (`UserService.UpdateAsync`/`DeactivateAsync`/`ChangeRoleAsync`/permissions) | Construits dans ce lot plutôt que redocumentés comme écart : nécessaires pour que l'audit des actions listées au §28.3 (changement de rôle, permission, désactivation, promotion/retrait Administrateur) porte sur des actions réellement exécutables, pas un point d'entrée mort | 2026-07-17 |
| Garde-fou "dernier administrateur actif" (CLAUDE.md §17, Lot 6) | Appliqué comme un invariant système (compte les autres administrateurs actifs, indépendamment de qui exécute l'action) plutôt que limité au seul cas littéral de l'auto-retrait | 2026-07-17 |
| Moteur d'import (§27, Lot 6) | Pipeline générique unique (aperçu/simulation/exécution) + un adaptateur typé par type important réutilisant exclusivement les services métier existants (aucun accès direct au contexte EF Core pour les entités importées) ; `CsvRequestBinder`/`FieldDiffer` génériques par réflexion (même principe que Mapster) plutôt que du code répété par type | 2026-07-17 |
| Portée du mode Complet par type importable (Lot 6) | Uniquement pour les types globaux non scopés à un parent et disposant déjà d'un mécanisme d'archivage exposé par leur service (Project, User, TimeEntry, Budget) — jamais pour un sous-type scopé (ProjectParticipant, Plannings), le moteur n'ayant pas de notion de périmètre de fichier | 2026-07-17 |
| Comparaison SharePoint simulée (§27.4, Lot 6) | Diff entre le fichier chargé et l'état actuel de la base (reflet du dernier import), pas un stockage fichier-à-fichier — réutilise le moteur de diff générique, pas de second mécanisme de comparaison | 2026-07-17 |
| Correction de données `VABE`/`VSR.IsRun` (règle métier validée, Lot 6) | Seedés à tort `IsRun: false` au Lot 3 (`Lot3Seed.cs`) alors que "les références INC/CHG/PRB/RITM/VABE/VSR sont des références RUN" ; corrigés à `true` via la migration Lot 6 (rétro-alimentation des lignes seedées, même mécanisme qu'au Lot 4) | 2026-07-17 |
| Révision de roadmap : phase « Frontend complet (V1) » avant l'industrialisation | Ancien Lot 7 (Industrialisation) renuméroté Lot 13 ; nouveaux Lots 7-12 construisant les écrans fonctionnels §8-§28 écran par écran sur l'API livrée aux Lots 1-6, décision actée par l'utilisateur à la clôture du Lot 6 (`docs/ROADMAP.md`) | 2026-07-17 |
| Bibliothèque de composants frontend (Lot 7) | **MUI (Material UI) v9**, plutôt que CSS Modules seul ou Ant Design — nécessite un thème dédié pour éviter le rendu Material par défaut, conformément au §8.1 (sobre, corporate) | 2026-07-17 |
| Bibliothèque d'icônes frontend (Lot 7) | **lucide-react** (tree-shakeable, licence ISC) | 2026-07-17 |
| Résolution GUID → code de permission côté frontend (Lot 7) | Nouvel endpoint minimal en lecture seule `GET /api/v1/permissions` (seul ajout backend du lot) — `UserDto.PermissionIds` (Lot 1) n'exposait que des GUIDs, aucun endpoint existant ne les résolvait en codes | 2026-07-17 |
| Sélecteur d'identité de démonstration (Lot 7) | Composant Header peuplé par `GET /api/v1/users`, identifiant choisi persisté en `localStorage` + intercepteur de requête `apiClient` — pilote l'en-tête `X-Demo-User` existant (Lot 2) sans introduire de nouveau mécanisme d'authentification | 2026-07-17 |
| Portée du filtrage de sidebar par permission (Lot 7) | Limitée aux 2 entrées où une garde serveur de niveau contrôleur non ambiguë existe déjà (`Budgets` → `FINANCIAL_DATA_VIEW`, `Imports` → `IMPORT_EXECUTE`) plutôt que d'inventer une règle non appliquée par l'API pour les 14 autres entrées | 2026-07-17 |
| Polices auto-hébergées (Lot 7) | `@fontsource-variable/inter` (Inter Variable) plutôt qu'un lien Google Fonts, pour rester compatible avec la contrainte hors-ligne du futur profil PortableDemo (Lot 13) | 2026-07-17 |
| `FinancialValue` (Lot 7) | Rendu basé exclusivement sur la présence/absence de la valeur reçue (`value: number \| null \| undefined`), jamais sur une vérification de permission cliente — la donnée non autorisée est simplement absente de la réponse API (CLAUDE.md §13, `docs/ARCHITECTURE.md` §3) | 2026-07-17 |
| `StatusBadge` (Lot 7) | Composant délibérément agnostique du domaine (`label`/`tone`) : chaque écran métier futur fournit son propre mapping statut → tonalité, pas de règle métier dans un composant transverse | 2026-07-17 |
| Cleanup Vitest/RTL entre tests (Lot 7) | Déclaré explicitement (`afterEach(() => cleanup())` dans `src/test/setup.ts`) plutôt que de dépendre de l'auto-cleanup implicite de `@testing-library/react`, qui ne s'active que si `afterEach` est global — ce projet garde `globals: false` (imports explicites, CLAUDE.md §9) | 2026-07-17 |
| Props CSS raccourcies sur `Stack`/`Typography` (MUI v9, constat technique Lot 7) | `alignItems`/`justifyContent`/`flexWrap`/`fontWeight`/`fontSize` ne sont plus acceptées en props directes depuis MUI v9 (changement d'API réel de la bibliothèque, pas un problème d'outillage) — systématiquement passées via `sx` dans ce lot et les suivants | 2026-07-17 |
| Révision de périmètre du Lot 8 (5 référentiels non prévus au cahier des charges) | Technologies, Clients, Types de projet, Centres de coûts, Devises validés un par un avec l'utilisateur avant implémentation et consignés dans `docs/BACKLOG_METIER.md` §5-9 (CLAUDE.md §5), avant toute écriture de code — exception explicite à la règle « aucune évolution backend » de la phase Lots 7-12 | 2026-07-20 |
| Liaisons Technology ↔ Application/Resource (Lot 8) | Remplacement complet par suppression puis recréation des lignes de jointure (`IRepository<T>.RemoveAsync`/`AddAsync` par « stub » de clé), jamais par réaffectation de la collection de navigation sur une entité récupérée via `Query()` (non suivie) — évite un bug latent de lignes orphelines, vérifié par un test dédié | 2026-07-20 |
| `CompanyType`/`Role` sans endpoint `GET` (Lot 8, écart backend pré-existant) | Valeurs seedées portées en dur côté frontend, centralisées dans `src/lib/knownReferentials.ts` (un seul endroit, jamais dupliqué) plutôt que dans chaque formulaire qui en a besoin | 2026-07-20 |
| Détail statistique de la fiche Application (Lot 8) | Réutilisation de `GET /api/v1/reporting/charges?applicationId=…` (Lot 5) plutôt qu'un nouvel endpoint d'agrégation dédié — ferme l'écart noté au Lot 1 sans évolution backend | 2026-07-20 |
| Coquille `ReferentialAdminTab` (Lot 8) | Liste + dialog de création/édition partagés par la majorité des onglets Administration ; formulaire (`renderCreateForm`/`renderEditForm`) toujours spécifique au référentiel, jamais généralisé au-delà de ce que les champs ont réellement en commun | 2026-07-20 |
| Périmètre de l'onglet Commandes (Administration, Lot 8) | Liste de consultation seule (référence/libellé/dates, aucun montant) — la gestion complète relève du Lot 11 (Commandes, Budgets et Jalons), non anticipée | 2026-07-20 |
| `TimeEntry.ApplicationId` (§19.1, Lot 9) | Option A retenue (validée avec l'utilisateur, 3.1) : ne pas ajouter le champ ce lot — écart documenté (`docs/BACKLOG_METIER.md` §11), pas une correction anticipée d'un modèle déjà figé | 2026-07-20 |
| Filtres serveur `GET /api/v1/time-entries` (§19.4, Lot 9) | Option B retenue (validée, 3.2) : trois nouveaux paramètres optionnels sur l'action déjà existante, colonnes déjà présentes depuis les Lots 3/4 — aucune nouvelle entité, aucune migration | 2026-07-20 |
| Modification d'une absence Brouillon (§23.2, Lot 9) | Option B retenue (validée, 3.3) : `PUT /api/v1/absences/{id}` restreint au statut Brouillon (409 sinon), amélioration UX volontairement acceptée en alternative à « annuler puis recréer » | 2026-07-20 |
| Ressource par défaut de l'écran Saisie des temps (Lot 9) | Ressource de l'identité de démonstration courante, sélecteur toujours disponible sans restriction non portée par le backend (validé, 3.4) | 2026-07-20 |
| Calcul de la capacité par ressource sur `/disponibilites` (Lot 9) | Approche N+1 conservée (un appel `GET /resources/{id}/availability` par ressource, `useQueries`) plutôt qu'un nouvel endpoint d'agrégation multi-ressources — acceptable au périmètre actuel, non anticipé (validé, 3.5) | 2026-07-20 |
| Sécurité de l'écran Disponibilités (Lot 9) | Aucune nouvelle règle de sécurité serveur — les filtres département/service/équipe reflètent uniquement les permissions déjà appliquées côté API (validé, 3.6) | 2026-07-20 |
| Semaine lundi-dimanche sur `/disponibilites` (Lot 9) | `periodBounds` chaîne explicitement `.locale('fr')` et importe `dayjs/locale/fr` localement, plutôt que de dépendre d'un `dayjs.locale('fr')` global jamais posé dans l'application (seul `LocalizationProvider adapterLocale="fr"`, Lot 7, existe pour les DatePickers MUI) | 2026-07-20 |
| KPI mensuel/annuel de `/mes-absences` (Lot 9) | Calculés exclusivement via `GET /resources/{id}/availability` (`AvailabilityService.JoursAbsenceValidee`, Lot 3) — jamais sommés côté frontend à partir des plages de dates brutes, qui compterait les week-ends à tort | 2026-07-20 |
| Filtre « semaine » de `/temps` (§19.4, revue de clôture Lot 9) | Fonction `weekBounds` extraite dans `lib/dateUtils.ts` et partagée avec `periodBounds` (`/disponibilites`) plutôt que dupliquée — un champ date ponctuel calcule les bornes lundi-dimanche et les applique à Du/Au existants, aucun nouveau state de filtre persistant | 2026-07-20 |
| Totalisation automatique de `/temps` (§19.4, revue de clôture Lot 9) | Requête dédiée sur `GET /time-entries` déjà existant (mêmes filtres, `pageSize=1000`), somme des heures côté client — pas un nouvel endpoint d'agrégation backend ; seuil documenté comme limite de démonstration, à remplacer par un agrégat serveur si le volume dépasse ce seuil | 2026-07-20 |
| Filtres étendus de `GET /api/v1/projects` (§16.1, validé, Lot 10 Décision 1) | 5 nouveaux paramètres optionnels sur l'action déjà existante ; `alerteBudget` matérialise puis pagine en mémoire (non traduisible en SQL), page vide sans `FINANCIAL_DATA_VIEW` plutôt qu'un filtre ignoré silencieusement | 2026-07-21 |
| Vue transverse "Planning projet" (§18.2, validé, Lot 10 Décision 2) | Nouvel endpoint `GET /api/v1/project-planning`, agrégation entièrement serveur — remplace une agrégation par N appels frontend, écartée pour rester compatible avec pagination/tri serveur corrects | 2026-07-21 |
| Lecture des lignes hebdomadaires (§18.3, validé, Lot 10 Décision 3) | `GET plan-versions/{versionId}/weekly-plans` ajouté — écart constaté (écriture seule depuis le Lot 4) | 2026-07-21 |
| `ProjectParticipant` sans Update (Lot 10 Décision 4) | Non ajouté ce lot — Create + Remove(désactivation) jugés suffisants, cahier des charges §17.2 ne liste que des champs à afficher, pas une action de modification explicite | 2026-07-21 |
| Temps/reste à faire par participant (§17.2, Lot 10 Décision 5) | Agrégation frontend sur `GET /time-entries?projectId=&resourceId=` déjà filtré par permission — même principe que la totalisation automatique du Lot 9, aucune nouvelle règle serveur | 2026-07-21 |
| « Avancement » (%) (§16.2, Lot 10 Décision 6) | Jamais calculé ni côté frontend ni côté serveur, faute de formule définie par le cahier des charges — affiché « — » | 2026-07-21 |
| « Commandes liées » (§16.2, Lot 10 Décision 7) | Dérivées à l'affichage en fiche détail (`Budget.OrderId`/`ProjectParticipant.DefaultOrderId`), pas de colonne en liste | 2026-07-21 |
| Gantt (Lot 10 Décision 8) | Non implémenté ce lot — seuls `Timeline`/`WeeklyPlanningGrid` construits, conformément à `docs/ROADMAP.md` ; reste au backlog | 2026-07-21 |
| Enrichissement du seed de démonstration (§35, Lot 10 Décision 9) | Limité aux axes Projets (8) et Jalons (15), conformément à la demande explicite de l'utilisateur — autres axes (temps, absences, sociétés) non enrichis ce lot | 2026-07-21 |
| `ProjectStatus` sans endpoint `GET` (Lot 10 Décision 10) | Corrigé par un vrai endpoint `GET /api/v1/project-statuses` (option A, validée) plutôt que contourné par `knownReferentials.ts` — le lot touchant déjà le backend, cohérent de ne pas accumuler un second contournement | 2026-07-21 |
| `OperationalRole` sans endpoint `GET` (écart backend pré-existant, constaté Lot 10) | Contourné via `knownReferentials.ts` (même pattern que `CompanyType`/`Role`, Lot 8) — écart non discuté avec l'utilisateur, donc pas étendu à la portée de la Décision 10 | 2026-07-21 |
| Refactoring `GetCoutReelConsommeAsync` (Lot 10, motif technique) | Déplacé de `ProjectService` vers `ProjectPlanningService` (privé) pour permettre à `ProjectService` de dépendre de `ProjectPlanningService` (filtre `alerteBudget`) sans dépendance circulaire — aucun changement de comportement, formule inchangée | 2026-07-21 |
| Évolution mensuelle et heatmap de charge (§25.3, Lot 12 Décision 1) | Extension de `GET /api/v1/reporting/charges` (agrégation supplémentaire sur des données déjà chargées) plutôt qu'un nouvel endpoint dédié — même service, même filtre | 2026-07-21 |
| Export du rapport opérationnel (§26.1, Lot 12 Décision 2) | `GET /api/v1/reporting/operational/export` ajouté, mirror exact du moteur `ExportService` déjà utilisé par Charges/Financial (Lot 6) | 2026-07-21 |
| Agrégation « Capacité vs réalisé » (Lot 12, révision de la Décision 3 initiale) | Composée côté frontend à partir de `DashboardOperationalKpisDto` déjà exposé (Lot 5) — recommandation initiale de construire un endpoint dédié explicitement révisée après investigation, avant tout développement, suite à la réserve de l'utilisateur | 2026-07-21 |
| Agrégation « Prévu vs réalisé » (§25.3, Lot 12 Décision 3, justifiée après la même réserve) | Construite côté backend (`BuildPlanComparisonAsync`, `ReportingService`) — 4 raisons vérifiées : aucune donnée déjà exposée, parité de filtres manquante sur `GET /project-planning`, agrégation sur données paginées non déjà chargées, règle « version Active uniquement » non dupliquable côté frontend sans réimplémenter `ProjectPlanningService` | 2026-07-21 |
| Mapping CSV → DTO des imports (§27.3 étape 5, Lot 12 Décision 4) | Confirmé en lecture seule, aucun assistant interactif construit — premier écran Imports du projet, décision reconduite du Lot 6 | 2026-07-21 |
| Authentification simulée sessionnée (§6.5, Lot 13 Décision 1) | Session serveur réelle (`UserSession`, cookie HttpOnly) plutôt qu'un en-tête rejoué sur chaque requête — toujours sans mot de passe, JWT ou écran de connexion réel ; `IAuthenticationProvider`/`DemoAuthenticationProvider` remplaçables par un futur provider LDAP/OIDC | 2026-07-22 |
| `UserSession.IsPersistent` (Lot 13, conception anticipée demandée) | Modèle prêt pour distinguer session navigateur/persistante ("se souvenir de moi") dès ce lot, fonctionnalité non exposée à l'écran — évite une migration supplémentaire quand elle sera construite | 2026-07-22 |
| Modèle RBAC (§6.1, Lot 13 Décision 2) | `RolePermission` (permissions par défaut du rôle) + `UserPermission.Effect` (`Grant`/`Revoke`, exception individuelle prioritaire) plutôt que permissions individuelles pures — calcul centralisé dans `PermissionResolutionService`, matrice de rôle dérivée des affectations déjà seedées sans régression | 2026-07-22 |
| Périmètre organisationnel (§6.3, Lot 13 Décision 3) | Explicitement différé à un lot dédié après validation Product Owner — aucune règle partielle introduite | 2026-07-22 |
| Pipeline GitHub Actions (Lot 13 Décision 4) | Deux jobs (backend/frontend), sans SonarQube/SonarCloud ni Docker (décision explicite), structure `workflow_call` pensée pour les accueillir plus tard sans refonte | 2026-07-22 |
| Nommage de l'abstraction d'authentification (Lot 13, ajustement demandé) | `IAuthenticationProvider`/`DemoAuthenticationProvider` plutôt qu'`ISimulatedAuthenticationProvider` — nom générique cohérent avec un futur remplacement LDAP/OIDC | 2026-07-22 |
| Seuils de couverture (Lot 13 Décision 4) | Mesurés réellement avant fixation (backend par projet via `dotnet test --collect`, frontend via `@vitest/coverage-v8`), jamais un chiffre inventé — fixés en dessous de la mesure avec marge de sécurité, `SafranTimeTracker.Migrations.*` exclus du calcul backend | 2026-07-22 |
| Correctif de sécurité dédié hors lot (élévation de privilèges) | `POST /api/v1/users` créait un utilisateur (rôle, permissions) sans aucune garde de permission, seule action de sécurité non gardée du contrôleur — `[RequirePermission(UserAdministration)]` ajouté, PR #9, tag `v0.13.1` | 2026-07-23 |
| Révision de roadmap (3ᵉ) : Lot 14 redéfini « Audit, Refactoring et Qualité » | Revue d'architecture complète du dépôt (Lots 0-13) menée en lecture seule avant l'ouverture du Lot 14, décision du Product Owner de fermer la dette/les incohérences/les risques constatés avant l'industrialisation plutôt qu'après ; ancien contenu du Lot 14 (packaging, déploiement, sauvegarde/restauration, documentation d'exploitation) renuméroté Lot 15 sans modification (`docs/ROADMAP.md`) | 2026-07-24 |
| Roadmap figée jusqu'à la V1 (Lots 15-19) | Sur la base du cahier des charges (§6.3, §14, §17.2, §27.3, §36, §37, §40), de `docs/BACKLOG_METIER.md` et du rapport d'audit du Lot 14 : Lot 15 « Complétude fonctionnelle & Administration » (renommé pour refléter son contenu réel — périmètre organisationnel, administration, référentiels, CRUD, plus large que le seul périmètre organisationnel), Lot 16 « UX, Responsive et Performance frontend » (promu lot obligatoire, ferme l'exigence §36.6 jusqu'ici non couverte), Lot 17 « Industrialisation et packaging » (ancien Lot 15/14/13/7, contenu inchangé), Lot 18 « Pré-production » (recette fonctionnelle §37, validation métier, tests utilisateurs, gel fonctionnel), Lot 19 « Mise en production V1 ». Risque non tranché signalé explicitement, pas résolu : authentification de démonstration acceptée pour la V1 ou authentification réelle requise — décision Product Owner attendue en Lot 18 (`docs/ROADMAP.md`) | 2026-07-24 |

Décisions techniques ouvertes à ce jour : restriction de la validation d'absence au seul responsable hiérarchique, restriction de la modification d'un projet/jalon au seul pilote/responsable, restriction des transitions de commande/budget/administration utilisateur/import/audit par périmètre organisationnel (Lot 13, Décision 3, différée explicitement), endpoints `GET` manquants pour `CompanyType`/`Role`/`OperationalRole`, écran de gestion des rôles/permissions par utilisateur (l'API le permet déjà), définition du domaine « Catégories » (reporté), assistant d'import interactif (mapping de colonnes, §27.3 étape 5), formule d'« avancement » (%, §16.2, Lot 10), Gantt (Lot 10, backlog), `Update` de `ProjectParticipant` (§17.2, Lot 10), migration de `RequirePermissionAttribute` vers les policies natives ASP.NET Core (Lot 13, non tranchée), nettoyage des sessions expirées (Lot 13, simplification assumée), extension de `[RequirePermission]` aux contrôleurs non gardés (Lot 13, proche du périmètre organisationnel) (voir « Écarts connus » ci-dessus).

## Comment mettre à jour ce document

1. À la fin de chaque lot : changer le statut (`Non démarré` → `En cours` → `Terminé`), mettre à jour la date, cocher les cases correspondantes.
2. Un lot ne passe à `Terminé` que s'il est compilable, testé réellement et démontrable (règle de méthode, `CLAUDE.md` §20).
3. Tout écart avec `docs/ROADMAP.md` ou le cahier des charges est consigné dans la section « Écarts connus ».
